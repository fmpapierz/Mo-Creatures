# Mo’Creatures 26.2 Port — Verified Gap Audit (2026-07-05)

> **STATUS 2026-08-17: FULLY CLOSED.** A 7-agent re-verification against current code found 41 of
> 51 checks already fixed by sessions after this audit's date; the remaining 10 (fishnet,
> fishing-hook capture, enableHunters, ostrich vials, F9 numeric GUI, lair tree variety,
> carried-pet render lag, kitty carry, adult scale cap, food values) were implemented 2026-08-17
> and verified in-game on Fabric. The 134-zero-thickness-box culling item (the "Outstanding
> decision" in TEST_PLAN.md) was resolved the same day by restoring backface culling to the
> Mo'Creatures models. Per-item annotations below are historical; do not re-verify from this file —
> read the current code.

Legacy 1.12.2 source vs the current Multiloader port. 7 parallel survey readers + adversarial per-gap verifiers (grepped the port to confirm each gap is genuinely absent, not just renamed). **42 confirmed gaps, 2 deliberate adaptations, 0 false positives.**

The port’s skeleton (all 46 creatures, items, blocks, dimension, spawning, and the big systems) is complete and works; what follows is the tail of per-species flavor behaviors, signature mechanics, config wiring, and visual FX that were simplified or dropped.

## HIGH impact (6)

### H1. Horse breeding genetics table (HorseGenetics) reduced to 50/50 coat pick
- **Area:** assets_data_meta — **verdict:** confirmed_partial
- **What’s missing/reduced:** The surveyor is correct on every point. The port's getBreedOffspring() collapses the flagship horse genetics system to a 50/50 coat inheritance with a 1/16 ordinary-coat mutation and a special-coat (>=21) gate requiring both parents primed. Genuinely lost vs legacy: (1) the ~40-entry hybrid-coat pairing table — specific ordinary crosses that produced coats 3,4,6,7,8,9,11,12,13,16,17 can no longer arise from breeding, so those coats are only obtainable by natural spawn/spawn-egg, not by the intended breeding progression; (2) zorse (61), mule (66), zonky (67) — zebra+horse, donkey+horse, zebra+donkey hybrids are unobtainable via breeding even though their textures ship (horsezorse/horsemule/horsezonky in getTexture switch); (3) white-fairy special breeding (unicorn 36 x white pegasus 39 -> white fairy 50 with BOTH parents vanishing) is entirely absent — no vanishHorse equivalent exists anywhere in the port; (4) legacy rare+rare -> random ordinary and rare+ordinary -> lower-type rules are replaced by the coarser >=21 essence gate. easyBreeding is read by config/command/GUI but the horse breeding path never consults it (legacy used it to skip the 25%/25% straight-inheritance shortcut). This is present-but-substantially-reduced, not fully missing (basic coat inheritance + special-coat gating do work), so 'partial' at high severity is the correct classification. Note: essence-based transforms into these hybrids DO exist (e.g. zorse->nightmare via fire essence), but that is a different mechanic from breeding them into existence.
- **Verified in port:** Port getBreedOffspring() at common/.../entity/passive/MoCEntityHorse.java lines 587-611 is exactly the reduced version: inherited = 50/50 parent pick; 1/16 mutation to random ordinary coat (nextInt(5)+1); isSpecialCoat (t>=21) blocked unless both parents' eatenPumpkin. No typeA/typeB combination table. Grep of whole port tree for 'Genetics|vanishHorse|whiteFairy|WhiteFairy' (*.java) returned NO files. In MoCEntityHorse.java, types 61/66/67/50 appear ONLY in essence-transform (fireEssence zorse->nightmare l.418/431), getTexture() render switch (l.689,700-703), and shadow/render helpers (l.725-764) — never inside getBreedOffspring(). Legacy MoCEntityHorse.HorseGenetics() (legacy lines 1145-1303) IS the full ~163-line Mendelian table: zorse (60+<21->61), mule (65+<21->66), zonky (60+65->67), whiteFairy (36+39->50 with both vanishHorse()), rare+rare->random 1-5, rare+ordinary->lower type, and ~40 hardcoded ordinary-coat pairings (1x2->6, 1x4->7, 2x4->3, etc.), respecting easyBreeding.

### H2. Lair terrain uses vanilla floating_islands blocks, not the mod's lair grass/dirt/stone
- **Area:** blocks_worldgen_dim — **verdict:** ~~confirmed_partial~~ **CLOSED (2026-08-10)** — stale. `data/mocreatures/worldgen/noise_settings/wyvern_lair.json` now exists with `default_block=mocreatures:stone_wyvern_lair` and surface rules placing `grass_wyvern_lair`/`dirt_wyvern_lair`, and `dimension/wyvern_lair.json` points `generator.settings` at it — exactly the fix prescribed below. The ogre_lair sibling dimension now exists as well (Ogre Prince pass). Historical text kept for the record:
- **What’s missing/reduced:** Confirmed partial (not actually_present, not adaptation). The Wyvern Lair dimension is functionally implemented — it generates floating islands with the correct teal sky/fog, biome, wyvern/bird/butterfly spawns, wyvern portal, mod trees (log/leaves_wyvern_lair), and decorative tall_grass_wyvern_lair. But the terrain surface and body are vanilla grass_block/dirt/stone (inherited unchanged from minecraft:floating_islands), NOT the mod's distinctive green grass_wyvern_lair / dirt_wyvern_lair / stone_wyvern_lair blocks that comprised the entire island in legacy. Those three blocks are registered (MoCBlocks.java) and obtainable in creative, but are never placed by generation. Player-visible impact is high because the lair's signature look (the whole ground being the greenish Wyvern-Lair grass over lair-stone) is lost; the ported lair looks like a generic vanilla floating-islands overworld with a colored sky. Fix: add a mocreatures:wyvern_lair noise_settings JSON (clone floating_islands, set default_block=stone_wyvern_lair) plus a surface_rule mapping top->grass_wyvern_lair and filler->dirt_wyvern_lair, and point the dimension's generator.settings at it. The ogre_lair block set having no dimension at all is a separate item beyond this claim's scope.
- **Verified in port:** common/src/main/resources/data/mocreatures/dimension/wyvern_lair.json sets generator.type=minecraft:noise, settings="minecraft:floating_islands" with NO override. Repo-wide grep for noise_settings across common/fabric/neoforge src returns only that one dimension file; there is no mocreatures noise_settings JSON and no SurfaceRules/custom ChunkGenerator (the only file in common/.../worldgen is MoCWyvernPortalFeature.java). floating_islands' default_block=stone, surface = vanilla grass_block/dirt. The biome (worldgen/biome/wyvern_lair.json) places only decorative mod blocks: tall_grass_wyvern_lair (configured_feature wyvern_lair_grass = simple_block) and log/leaves_wyvern_lair (wyvern_lair_tree); the tree even uses "minecraft:dirt" for below_trunk_provider. No feature or rule places grass_wyvern_lair/dirt_wyvern_lair/stone_wyvern_lair as terrain. Legacy MoCChunkProviderWyvernLair fills the body with mocStone and BiomeGenWyvernLair sets topBlock=mocGrass, fillerBlock=mocDirt (confirmed in legacy-source).

### H3. Firefly glowing belly (additive glow render pass)
- **Area:** client_render_fx — **verdict:** confirmed_missing
- **What’s missing/reduced:** The firefly renders as a flat, normally-lit model. Its signature additive glowing abdomen (fireflyglow.png at full-bright, additive-blended) is entirely absent — the texture asset ships but is wired to nothing, and MoCMobRenderer has no render-layer support to host such a pass. Player-visible impact is high for the firefly's iconic feature: at night the firefly is a plain dark bug instead of a glowing point of light. To restore, a RenderLayer subclass (analogous to vanilla EyesLayer) forcing packedLight to LightTexture.FULL_BRIGHT and rendering fireflyglow.png via RenderType.eyes()/an additive-emissive RenderType would need to be added to the firefly renderer (requires either a firefly-specific MoCMobRenderer subclass or a generic glow-layer hook).
- **Verified in port:** Legacy MoCRenderFirefly.setTailBrightness() binds fireflyglow.png as a 2nd pass with GL_ONE/GL_ONE additive blend + full lightmap (char 61680 = 0xF0F0). In the port: MoCreaturesClient.java:101 registers FIREFLY with plain `new MoCMobRenderer<>(...)` and no render layer; MoCMobRenderer.java (read fully, 84 lines) contains NO addLayer/RenderType.eyes/emissive/packedLight code; MoCModelFirefly.java has no glow/brightness logic; MoCEntityFirefly.java has no brightness override. `grep fireflyglow` over the entire port returns only the resource file itself (common/src/main/resources/assets/mocreatures/textures/models/fireflyglow.png) — referenced by ZERO Java files. No custom firefly renderer subclass exists (find *RenderFirefly*/*FireflyRenderer* = none). Neoforge and fabric modules: grep for fireflyglow/RenderLayer/GlowLayer/EyesLayer = no matches. The only glow/emissive code in the port is in MoCGolemRenderer/MoCModelGolem, unrelated to the firefly.

### H4. Behavior config flags defined but never honored by gameplay (dead settings surface)
- **Area:** config_cmd_gui_hud — **verdict:** confirmed_missing — **partially stale (2026-08-10):** the ogre trio is no longer dead: `ogreStrength`/`fireOgreStrength`/`caveOgreStrength` are consumed as smash radii at `MoCEntityOgre.java:223` (and by the Ogre Prince), and `ogreAttackRange` at `MoCEntityOgre.java:112`. The rest of the flag inventory below has not been re-verified.
- **What’s missing/reduced:** CONFIRMED, with one refinement to the surveyor's field accounting. The core claim is correct: the config/command/GUI expose ~19 boolean toggles + ~13 numeric tunables, but gameplay honors only maxAnimals/maxMobs/maxWaterMobs/maxAmbient (MoCMobCap), enableOwnership/maxTamed/maxOPTamed (MoCAnimal), and the per-entity spawn.* keys (MoCSpawns). Legacy MoCProxy.readConfigValues() (~lines 1142-1193) read ~33 tunables that gameplay consulted live (elephantBulldozer, easyBreeding, attackDolphins/Horses/Wolves, spawnPiranhas, zebraChance, staticBed/Litter, enableResetOwnerScroll, destroyDrops, particleFX, animateTextures, displayPetIcons, three ogre strength radii, etc.). In the port every one of those behavior/strength flags is dead: toggling them via the F9 GUI or /moc config saves to mocreatures.properties but changes nothing in-game. Refinement: the surveyor listed displayPetIcons as dead (correct — MoCPetHud never reads it) but displayPetName and displayPetHealth ARE honored (MoCPetHud.java line 51), so the HUD-name/health pair is the sole exception the surveyor may have implicitly folded in; it does not weaken the verdict. Fix scope is per-flag: add the missing gate at each site (e.g. `&& MoCConfig.get().elephantBulldozer` at MoCEntityElephant.java:222; `if (!MoCConfig.get().enableResetOwnership) return PASS;` in MoCScrollItem OWNER/SALE modes; consult zebraChance in horse breeding; guard displayPetIcons/particleFX/animateTextures in their render paths; consult ogre/cave/fireOgreStrength as destruction radii; honor attack* in the water/monster targeting goals). Player-visible impact is high because the settings UI and /moc command visibly present ~26 switches that silently do nothing.
- **Verified in port:** Project-wide grep (common+fabric+neoforge *.java) for the 20 behavior/strength flags (elephantBulldozer, attackDolphins/Wolves/Horses, spawnPiranhas, zebraChance, staticBed, staticLitter, enableResetOwnership, destroyDrops, destroyPassiveDrops, particleFX, displayPetIcons, animateTextures, easyBreeding, easyHarvesting, ogreStrength, caveOgreStrength, fireOgreStrength, sharkStrength, modifyVanillaSpawns) matched ONLY 3 files: config/MoCConfig.java (definitions), command/MoCCommand.java and client/gui/MoCSettingsScreen.java (the config surface). No gameplay/entity/spawn file reads any of them. The full set of MoCConfig.get() consumers is: MoCMobCap (maxAnimals/Mobs/WaterMobs/Ambient), MoCAnimal (enableOwnership/maxTamed/maxOPTamed), MoCSpawns (per-entity spawn keys), MoCPetHud (displayPetName/displayPetHealth). Spot-checks confirm the flags are ignored: MoCEntityElephant.java line 222 gates bulldozing on `getTusks() > 0` only (no elephantBulldozer check); MoCScrollItem.java lines 40-65 execute all three reset/free-ownership modes unconditionally (never reads enableResetOwnership); MoCEntityHorse.getBreedOffspring (lines 587-611) never references zebraChance and cannot ever produce a zebra foal.

### H5. Ostrich: chest, flag, helmet armor, vial transforms, egg-guarding AI, unicorn buckle
- **Area:** entities_ai — **verdict:** confirmed_partial
- **What’s missing/reduced:** Verdict is confirmed_partial: the ostrich exists and is rideable (ride + per-type speed + sprint-stamina + head-in-sand hiding + solo egg-laying are all present and faithful), but six legacy feature clusters are genuinely absent from the port:

1. Chest storage: legacy interact() gives a MoCreatures.key and a MoCAnimalChest("OstrichChest", 9/18) opened via displayGUIChest, with NBT Items save/load and drop-on-death. Port has no SimpleContainer/menu, no key handoff, no getIsChested (dataWatcher slot 27) — grep empty.

2. Colored flag: legacy dyes a flag via Block.cloth (setFlagColor byte, dataWatcher 26) and dropFlag(). Port has no getFlagColor/setFlagColor — grep empty.

3. Helmet armor (12 tiers): legacy setHelmet 1..12 (leather/iron/gold/diamond + hide/fur/croc + 4 scorpion helms) with attackEntityFrom damage reduction of 1..4 points and dropArmor(). Port has no helmet field, no damage-reduction override, no armor drop — grep empty.

4. Vial transforms: legacy interact() consumes vialdarkness/vialundead/viallight/vialnightmare to transform(6/7/8/5) into black-wyvern/undead/unicorned/nether ostrich with a transformCounter flicker animation and 'transform'/'drinking' sounds. Port getTexture() still maps types 5-8 to ostriche/f/g/h but nothing ever calls setTypeMoC(5-8) and there is no transform() method — the higher variants are unreachable dead code.

5. Egg-guarding pair AI: legacy tamed female finds getClosestMaleOstrich, both setEggWatching(true), path to and protect the egg, and retaliate against egg-stealing players (entityToAttack + flapWings). Port lays a solo egg on a random tick with no watching, no male pairing, no theft retaliation — grep for getEggWatching/getClosestMaleOstrich empty.

6. Unicorn buckle: legacy type-8 ridden ostrich calls MoCTools.buckleMobs during a sprint to charge/knock mobs, and type-specific drops (unicorn horn, heartfire/heartdarkness/heartundead, rotten flesh). Port has no buckle (grep empty) and MoCBehavior only drops OSTRICHRAW.

The port is capable of these (elephant has working chest storage), so these are omissions rather than platform limits. Player-visible impact is high: the ostrich is reduced from a deep armored/storage/transform mount to a plain fast bird.
- **Verified in port:** Read full port MoCEntityOstrich.java (207 lines, C:\Users\warwa\ModDev\Mo' Creatures 26.2 Multiloader\common\src\main\java\drzhark\mocreatures\entity\passive\MoCEntityOstrich.java): only SPRINTING stamina, fireImmune(type5), head-in-sand hiding, and a lay-egg tick. NO interact/mobInteract override at all. Grep across the entire multiloader repo for setHelmet|getHelmet|setFlagColor|getFlagColor|OstrichChest => "No files found". Grep getEggWatching|setEggWatching|getClosestMaleOstrich|buckleMobs|buckle => "No files found". Grep vial* => only MoCEntityWyvern.java (wyvern evolution, unrelated). Base class MoCAnimal.java has no chest/helmet/flag/vial/transform members (grep empty). Whip item (MoCWhipItem.java line 56) does only whipSprint()/toggle-hiding for ostrich. In the ostrich file setTypeMoC(5/6/7/8) is never called and there is no transform() method, so texture cases 5-8 (ostriche/f/g/h) in getTexture() are dead wiring. Contrast: MoCEntityElephant.java DOES implement container storage, so the port is capable of it — the ostrich simply omits every one of these features.

### H6. Scorpion swords & stings apply no on-hit potion effects
- **Area:** items — **verdict:** confirmed_partial
- **What’s missing/reduced:** Partial, not fully missing: the weapons exist and function as diamond-tier swords with correct base damage (3.0, -2.4 speed matching diamond sword), so they are usable and craftable. What is reduced is the entire elemental identity layer: (1) no on-hit MobEffect is applied — dirt/frost/nether/cave swords and stings all behave as identical plain diamond swords, losing legacy poison/frost-slowdown/nether-fire/cave-confusion; (2) stings are not registered fragile (legacy ~10x wear), so they no longer shatter fast. The base weapon works but its signature elemental behavior — the reason these eight items exist as distinct items — is absent. Fix: introduce a custom weapon Item overriding the 26.2 post-hit hook to apply the mapped MobEffect (poison/movement-slowdown/nausea/blindness) or igniteForSeconds for nether, and set reduced durability on the four sting variants.
- **Verified in port:** All 8 scorpion weapons are registered as plain inert swords in MoCItems.java lines 360-375, e.g. SCORPSWORDDIRT = new Item(new Item.Properties().sword(ToolMaterial.DIAMOND, 3.0F, -2.4F)...), with no custom item class. `ls common/.../item/` shows NO weapon class (only Amulet/BuilderHammer/CreaturePedia/FishBowl/Furniture/NetherCannon/Rope/Scroll/StaffPortal/StaffTeleport/ThrownEgg/Whip/WoolBall). Grep for hurtEnemy/postHurtEnemy/specialWeaponType/WeaponType across common+fabric+neoforge Java returned zero hits tied to these items (the only MobEffects reference near scorpions is MoCEntityScorpion.java's living-mob sting attack, not the weapons). No datapack/enchantment/data-component in resources applies effects — resource hits are only client models/textures/lang. Legacy MoCItemWeapon.hitEntity() (legacy-source/.../item/MoCItemWeapon.java lines 61-85) switches specialWeaponType: 1=poison, 2=moveSlowdown, 3=setFire(10), 4=confusion, 5=blindness; stings registered breakable.

## MEDIUM impact (18)

### M1. Wild donkeys (type 65) and zebras (type 60) never spawn
- **Area:** assets_data_meta — **verdict:** confirmed_missing
- **What’s missing/reduced:** Confirmed genuine gap. Legacy natural horse spawns roll donkey (type 65, ~9%) and zebra (type 60, ~1%) via selectType(), scaled by the zebraChance config; the port's selectType() only ever produces ordinary coats 1-5. No alternate spawn path (finalizeSpawn, spawn event handler, registry) assigns 60/65 anywhere in common/fabric/neoforge. Player-visible effects: (1) wild zebras and donkeys never appear in any biome, so they are unobtainable without commands/essence; (2) this severs the only in-world source of zebra/donkey parents, breaking the mule/zorse/zonky breeding chain; (3) the ported zebraChance config knob is dead code with respect to spawns (declared, parsed, and serialized but never consulted by any spawn logic). Everything else about these coats (textures 60/65/67, bagger flags, sounds, essence branches) is present, which is why grep finds the types elsewhere - but the spawn origin is missing. Fix would restore the legacy weighted roll in selectType() (roll 0-99, map 90..(98-zebraChance) to donkey and the remainder to zebra, honoring MoCConfig.zebraChance).
- **Verified in port:** Port MoCEntityHorse.selectType() (common/.../entity/passive/MoCEntityHorse.java:649-654) is the sole natural-spawn coat assignment and only does `setTypeMoC(this.random.nextInt(5) + 1)` -> types 1-5 only. Grep of entire port (common/fabric/neoforge *.java) for `setTypeMoC(60/65)` and `zebraChance` shows type 60/65 appear only in getTexture (699/701), isBagger (139), essence transforms, and sounds - never a spawn roll. The `zebraChance` field exists in MoCConfig.java (105/180/409/475) but is never read outside the config class itself: it has ZERO effect on spawning. Legacy MoCEntityHorse.selectType() lines 2638-2681 explicitly rolls setType(65) donkey at `j <= 99 - zebraChance` and setType(60) zebra in the else branch, both scaled by proxy.zebraChance.

### M2. Continuous age-based creature size scaling (getSizeFactor)
- **Area:** client_render_fx — **verdict:** confirmed_partial
- **What’s missing/reduced:** The surveyor is correct. Continuous age-based size scaling is genuinely reduced to a binary 0.5x-baby / 1.0x-adult step in the port. The underlying age data (getMoCAge, the port's edad equivalent) IS present and persisted, but it is never wired into the renderer — no getSizeFactor/getScale hook exists anywhere in common/fabric/neoforge Java. Player-visible regressions vs legacy: (1) crabs, turtles, komodos, wyverns, jellyfish, and butterflies no longer grow smoothly from newborn to adult — they pop from 0.5x straight to full size when isBaby flips; (2) donkeys no longer render at the legacy 0.9x (MoCHorseRenderer has no scale override, so donkeys render identical in size to horses); (3) baby horses grow by the binary step rather than the continuous edad*0.01 curve. Classify as PARTIAL: the feature scaffolding (baby scaling + a per-entity age counter) is present, but the continuous per-species growth curve and the donkey size distinction are lost. Fix would be to add a size-factor hook on IMoCEntity/render state fed from getMoCAge and per-species overrides, then multiply in MoCMobRenderer.scale() (and add a donkey branch in MoCHorseRenderer). Medium severity: visible across many creatures but purely cosmetic, no gameplay/functional impact.
- **Verified in port:** MoCMobRenderer.scale() (common\...\client\renderer\MoCMobRenderer.java:72-78) only does `if (state.baby) poseStack.scale(0.5F,0.5F,0.5F)` after super.scale — a binary baby/adult step. Grep for getSizeFactor/getScale/renderScale/SizeFactor across the whole port returns ZERO real hits (only Vec3.scale motion math in Wyvern/Horse, and an unrelated axis-flip `poseStack.scale(-0.5F,-0.5F,0.5F)` in MoCGolemRenderer.java:115 for block cubes). IMoCEntity.java and MoCEntityRenderState.java have NO size/scale field. MoCHorseRenderer.java is a bare 6-line subclass with NO scale() override, so it inherits the binary 0.5x baby step and applies no donkey 0.9x. The continuous age counter exists on the entity (MoCAnimal.getMoCAge()/setMoCAge(), synched AGE data, persisted as "AgeMoC") but is never consumed by any renderer for sizing. Legacy source confirms the intended behavior: MoCEntityAnimal.java:1810 getSizeFactor() default 1.0F, overridden in MoCEntityCrab.java:129 (0.7f*getEdad()*0.01f), plus Turtle/Komodo/Wyvern (edad*0.01) and MoCRenderNewHorse.stretch (baby edad*0.01, donkey *0.9), applied via GL11.glScalef in MoCRenderAnimal.java:157.

### M3. JellyFish translucency, additive glow, and bell-pulse animation
- **Area:** client_render_fx — **verdict:** confirmed_partial
- **What’s missing/reduced:** Surveyor is correct — this is a genuine partial regression, not a renamed feature. The port jellyfish renders opaque and non-glowing, and its bell never pulses/contracts. Concretely absent vs legacy: (1) TRANSLUCENCY — legacy draws the body at ~0.7 alpha with a color tint; port uses the default opaque MobRenderer render type. (2) ADDITIVE BIOLUMINESCENT GLOW — legacy's isGlowing() branch switches to additive blending (glBlendFunc 770,1); the port has no isGlowing field/method at all and no additive render path. (3) BELL-PULSE PROPULSION ANIMATION — legacy's pulse() scales the whole model by pulsingSize (which ramps 0.0->0.4 each swim stroke, resetting per stroke), giving the signature contract/expand swim; the port has neither the pulsingSize field on the entity nor any scale-modulation in the model/renderer. Only the tentacle limb-swing survives (setupAnim rotating legs by walkAnimationSpeed). Player-visible impact: the jellyfish looks like a solid opaque blob that glides rigidly rather than the see-through, softly glowing, rhythmically pulsing creature of the legacy mod. To restore: add pulsingSize + isGlowing to MoCEntityJellyFish, expose them on MoCEntityRenderState, and give the jellyfish a dedicated renderer (or override in MoCMobRenderer) that selects a translucent/additive render type and applies the pulse scale in scale().
- **Verified in port:** Grep for `pulsingSize|isGlowing` across the ENTIRE port (common/fabric/neoforge) returns "No files found". Port entity common/.../entity/passive/MoCEntityJellyFish.java has no pulsingSize field and no isGlowing() (only stingCooldown + selectType + getTexture). Model MoCModelJellyFish.setupAnim (lines 126-147) only sets xRot on the 13 tentacle legs from walkAnimationSpeed — no bell scale/pulse, no alpha. JELLYFISH is registered with the plain generic MoCMobRenderer at MoCreaturesClient.java:95 (no subclass). MoCMobRenderer.java (renderer/MoCMobRenderer.java) extends MobRenderer with a default opaque render type; its scale() override only does baby 0.5x scaling — no GL_BLEND, no translucent/additive render type, no pulse. The ONLY entityTranslucent render type in the port's client models is on MoCModelFishBowl.java:34, unrelated to jellyfish. Legacy MoCRenderJellyFish.doRenderLiving (legacy-source) uses glBlendFunc(770,771)+glColor4f alpha 0.7, additive glBlendFunc(770,1) under isGlowing(), and pulse() scales the bell via entity.pulsingSize.

### M4. Wraith / FlameWraith ghostly translucency + glow
- **Area:** client_render_fx — **verdict:** confirmed_missing
- **What’s missing/reduced:** Legacy MoCRenderWraith.doRenderLiving wraps super in GL_BLEND and always renders wraiths at 0.6 alpha with a gray tint (glBlendFunc 770,771; glColor4f 0.8,0.8,0.8,0.6) — the additive "glow" branch (770,1) is actually DEAD code because `flag` is hardcoded `false`, so the flame-wraith additive glow never fired even in legacy. The real, player-visible legacy effect is the 0.6-alpha ghostly translucency common to both wraith types. The port drops this entirely: both render as fully solid opaque mobs via the generic MoCMobRenderer, so the spectral see-through look is lost. Fix: give the wraith a translucent RenderType (e.g. RenderTypes.entityTranslucent like the fish bowl model already uses) via a getRenderType override or a dedicated renderer subclass. Classified missing (not partial) because no translucency/glow code exists for these entities at all. Severity medium: purely cosmetic — no gameplay impact, but a distinctive signature look of these Wyvern-Lair undead is absent.
- **Verified in port:** MoCreaturesClient.java lines 111 & 113 register WRAITH and FLAME_WRAITH with plain `new MoCMobRenderer<>(...)`. MoCMobRenderer.java (common/.../client/renderer/) has NO getRenderType override — it extends MobRenderer with default opaque entity RenderType. A grep for getRenderType|translucent|additive|blend|isGlowing across common+fabric+neoforge client code returns only ONE hit: MoCModelFishBowl.java (entityTranslucent), which is unrelated. No MoCRenderWraith subclass exists in the port. The wraith entity classes contain zero glow/blend/render logic.

### M5. Undead-horse decay wisp FX (UndeadFX / FX_UNDEAD)
- **Area:** client_render_fx — **verdict:** confirmed_missing
- **What’s missing/reduced:** Genuine gap, not a rename. The FX_UNDEAD particle is registered and given a client provider (so the asset pipeline is complete) but is emitted NOWHERE — classic dead code. In legacy, adult undead/skeleton passive horses (types 23-25) emit a continuous greenish wisp while aging toward their next decay stage (MoCEntityHorse line 2222-2238), and the hostile undead horse mob (type 23) emits the wisp continuously (MoCEntityHorseMob line 299-302). In the port, undead passive horses and the undead horse mob emit no particles at all; the only horse FX is vanilla FLAME on the nightmare (type 38). Player-visible impact: undead horses look inert instead of shedding the signature decay wisp. Recommend adding a client-side FX_UNDEAD emission in MoCEntityHorse.tick() gated on isUndead()&&getIsAdult() (and in MoCEntityHorseMob) to restore parity, mirroring the existing nightmare FLAME block at MoCEntityHorse.java:266-274. Severity medium: purely cosmetic, but it is the intended visual identity of an entire coat family and the plumbing is already 90% present (only the spawn call is missing).
- **Verified in port:** Grep for FX_UNDEAD across the entire multiloader tree returns only 3 hits, all registration/provider, never an emission: registry\MoCParticles.java:32 (registers "fx_undead"), client\MoCParticleFactories.java:35 (ParticleProviderRegistry.register client provider), and resources\...\particles\fx_undead.json (texture def). No sendParticles/addParticle(FX_UNDEAD...) call exists anywhere. MoCEntityHorse.tick() (lines 266-274) emits ONLY vanilla ParticleTypes.FLAME, gated on getTypeMoC()==38 (nightmare); the only other emission is whipCrack() (line 635), also FLAME for nightmare. MoCEntityHorseMob.java has zero particle/addParticle/UndeadFX matches (grep empty). Legacy MoCEntityHorse.java:2237 calls UndeadFX() in the client aging branch when isUndead()&&type<26&&getIsAdult()&&rand(20)==0, and MoCEntityHorseMob.java:301 calls it when type==23&&rand(50)==0&&!isServer(); MoCClientProxy.UndeadFX (line 327) spawns MoCEntityFXUndead greenish wisps. None of that call path exists in the port.

### M6. /moc admin subcommand tree replaced with a much smaller, non-equivalent set
- **Area:** config_cmd_gui_hud — **verdict:** confirmed_partial
- **What’s missing/reduced:** The claim holds. The legacy per-entity spawn-tuning and biome-placement command surface is genuinely absent from the port. MoCCommand.java's own class javadoc admits the reduction ("the surface here is deliberately smaller"). What legacy exposed and the port dropped: (1) per-entity spawn weight tuning /moc <entity> frequency|min|max <int>; (2) biome placement /moc <entity> biomegroup add|remove <group> and /moc list biomegroups — the port has no biomeGroup mechanism in MoCSpawns at all; (3) numeric tunables spawntickrate/despawntickrate/zebrachance/ogre strengths & chances/maxtamedperop/perplayer. The port's /moc config accepts 19 boolean flags (a real subset of legacy booleans) and adds two new adaptations (spawn, count). Verdict is confirmed_partial rather than confirmed_missing because a /moc command with equivalent boolean-config and reload capability does exist and is functional; it is the per-entity/biome/numeric admin tuning that is non-equivalent. Leaning partial over deliberate-adaptation because the dropped capabilities (retuning spawn weights and biome placement from chat) have no replacement anywhere — not just a different command shape. Net player/admin impact: an operator cannot retune per-entity spawn rates or biome assignment at runtime as they could in 1.12.2.
- **Verified in port:** Port has exactly ONE /moc registration: common/.../command/MoCCommand.java register() (lines 52-79) builds Commands.literal("moc") with only 4 children: reload, count, spawn <entityId> [count], config <flag> [true|false]. MoCreatures.java line 49-50 wires only MoCCommand.register(dispatcher) into CommandRegistrationEvent; no other command class exists (Glob **/command/**/*.java returns only MoCCommand.java). Legacy CommandMoCreatures.java static block (lines 35-70) registers ~36 subcommands: per-entity biomegroup add/remove, frequency/min/max, list biomegroups, list <entity> biomegroups, spawntickrate, despawntickrate, despawnvanilla, worldgencreaturespawning, maxambient/maxmobs/maxanimals/maxwatermobs, usecustomspawner, caveogrestrength/fireogrestrength/ogrestrength/caveogrechance/fireogrechance/ogreattackrage, maxtamedperop/perplayer, zebrachance, debuglogging, enableresetownerscroll. Grep for biomegroup/frequency/spawntickrate/zebrachance/caveogre/maxtamedper across the port's command layer finds NONE as subcommands (matches are in config/spawn logic only); MoCSpawns.java has no biomeGroup concept at all (grep "biomeGroup" = no matches).

### M7. F9 settings GUI reduced from a full multi-tab spawn/config editor to a flat 19-toggle list
- **Area:** config_cmd_gui_hud — **verdict:** confirmed_partial
- **What’s missing/reduced:** Confirmed partial, matching the surveyor. The port's F9 screen (MoCSettingsScreen) is a flat grid of 19 boolean on/off CycleButtons plus Done — nothing else. The legacy F9 GUI (MoCSettings + MoCClientProxy) was a multi-panel tuning surface: General Settings, per-category creature/monster/watermob/ambient settings, CustomSpawner controls, seven biome-group editors, per-entity spawn frequency/min/max numeric editing, entity->biome-group assignment, an in-GUI Insta-Spawner, and reset-to-defaults. All numeric tunables and per-entity spawn/biome editing are absent from the port's GUI, and the Insta-Spawner is gone entirely (no InstaSpawner class anywhere in common/fabric/neoforge). Mitigating factor keeping this medium not high: the numeric config still exists in MoCConfig and remains adjustable out-of-game via mocreatures.properties and via the /moc command, so functionality isn't lost outright — only the in-game GUI editing surface is. Player-visible impact: F9 no longer lets players tune spawn frequencies, caps, ogre/shark strengths, zebra chance, biome groups, or insta-spawn creatures from within the game.
- **Verified in port:** Port's only settings screen is common/.../client/gui/MoCSettingsScreen.java: constructor adds exactly 19 CycleButton.onOffBuilder boolean flags in a 2-col grid + a Done button; no EditBox/numeric fields, no tabs, no per-entity/biome/insta-spawner panels. F9 routes only here: MoCKeyMappings.java:44 `mc.setScreenAndShow(new MoCSettingsScreen())`. Grep of the whole port for InstaSpawn/InstaSpawner/numberToSpawn = No matches. Grep for EditBox/spawnFrequency/packSize across client/ = 0 hits (only MoCNameScreen matched TextField). Yet MoCConfig.java DOES hold numeric tunables absent from the GUI: ogreStrength/caveOgreStrength/fireOgreStrength (l.97-103), sharkStrength (105), zebraChance (107), wyvernEggDropChance/monsterEggDropChance (107-109), maxAnimals/maxMobs/maxWaterMobs/maxAmbient/maxTamed/maxOPTamed (112-122), plus per-entity spawnFrequency/spawnMin/spawnMax(String) methods (260-270). Legacy MoCClientProxy.java confirms the far richer original: WidgetClassicTwocolumn panels for General/Custom-Spawner/7 biome groups (Forest/Arctic/Normal/Mountain/Jungle/Desert/Swamp), widgetInstaSpawnerColumn (l.663), entityList/entityBiomeList (582-585), and MoCSettings.java:189 edits `entityData.frequency = Integer.parseInt(...)`.

### M8. Biome-based coat/type selection dropped for all creatures (snow leopard, polar bear, snow fox, biome snakes, cave/nether/frost scorpions unreachable)
- **Area:** entities_ai — **verdict:** confirmed_missing
- **What’s missing/reduced:** CONFIRMED. The legacy mod's per-species checkSpawningBiome() (called from each selectType()) is entirely absent from the port — no method, no biome/temperature read, no biome-conditioned setTypeMoC anywhere in common/fabric/neoforge. Concrete player-visible consequences: the snow leopard (BigCat type 6 / bcleopard.png), polar bear (Bear type 4 / bearpolar.png), snow fox (Fox type 2 / foxsnow.png), and the cave/nether/frost scorpion variants (Scorpion types 2/3/4) can NEVER appear in normal survival — their case-labels and textures ship but are dead code reachable only via /moc or NBT. Snakes still show all 8 colours (they were pure-random-selectable even in legacy) but lose biome appropriateness AND the legacy desert-only spawn gate for the rattlesnake. So this is 'missing' rather than 'partial': the biome-selection mechanism does not exist at all. I set severity MEDIUM rather than HIGH because (a) all base variants still spawn and look correct, (b) the affected skins already ship and render fine when the type is set, and (c) it is a spawn-distribution/immersion regression (wrong-biome coats, a handful of unreachable variants) rather than a broken or crashing feature. Note a texture-naming quirk unrelated to the gap: port BigCat case 4 is bcsnowleopard.png (commented 'cheetah slot') while case 6 bcleopard.png is the legacy biome-locked snow leopard — either way type 6 is unreachable. Fix would be an overridden finalizeSpawn (or a helper called from selectType with spawn context) reading level.getBiome(pos) climate/temperature and tags to assign the cold/desert/depth variants, mirroring legacy checkSpawningBiome.
- **Verified in port:** Grep across the entire port (common/fabric/neoforge *.java) for checkSpawningBiome|getBiome|.temperature|BiomeTags|climateSettings|Holder<Biome> returns ZERO entity-variant hits. The only biome usages are MoCSpawns.java:73 (BiomeTags.IS_OVERWORLD — gates WHERE mobs spawn, never assigns TypeMoC) and two renderers (MoCGolemRenderer.java:75, MoCThrowableRockRenderer.java:50 — cosmetic block-color state). Verified each selectType() by reading the source:
- MoCEntityBigCat.java:38 picks i∈{1,2,3,4,7,5} — never 6 (getTexture() case 6 = bcleopard.png snow-leopard, unreachable).
- MoCEntityBear.java:32 picks {1,2,3} — never 4 (getTexture() case 4 = bearpolar.png, unreachable).
- MoCEntityFox.java:31 always sets 1 — never 2 (getTexture() case 2 = foxsnow.png, unreachable).
- MoCEntitySnake.java:32 pure random 1-8, no biome gate (legacy also GATED desert-only for type 7).
- MoCEntityScorpion.java:78 always sets 1; its finalizeSpawn (line 85) only sets babies, never a variant — getTexture() cases 2/3/4 (cave/nether/frost) unreachable.
Legacy source has checkSpawningBiome() in all five: MoCEntityBigCat.java:457 (setType(6) when currentbiome.temperature<=0.05F), MoCEntityBear.java:441 (setType(4) in Taiga/Ice/Frozen), MoCEntityFox.java:158 (setType(2) when temperature<=0.05F), MoCEntitySnake.java:827 (setType(7) rattlesnake in Desert + biome spawn-gating), MoCEntityScorpion.java:554 (setType(3) nether, setType(4) frost biome, setType(2) cave when posY<50 && !canBlockSeeTheSky).

### M9. Wild Wyvern no longer attacks or poisons players
- **Area:** entities_ai — **verdict:** confirmed_partial
- **What’s missing/reduced:** The Wyvern entity is present and functional as a flying/tameable/rideable mount, but its wild dangerous-predator facet is entirely absent — not reduced. There is zero attack code: no target acquisition, no melee damage, no poison effect, and the registered wyvernpoisoning sound is dead. Note the port DOES implement poison-on-hit for many other creatures via doHurtTarget (scorpion, bee, komodo, snake, jellyfish, pet scorpion) and hostile targeting via the wildHostile flag, so the omission is specific to the wyvern's missing .hostile() flag plus a missing doHurtTarget override — not a framework limitation. To restore parity: add .hostile() to the wyvern spec (gated to untamed) and a doHurtTarget override applying MobEffects.POISON ~200t at 1/3 chance with type>=5 doing 10 dmg. Classified partial (not missing) because the entity itself exists; the combat/threat behavior is the missing slice.
- **Verified in port:** MoCEntityWyvern.java registerGoals() (lines 96-102) adds ONLY FloatGoal — no MeleeAttackGoal/target goals — and the class has no doHurtTarget override and no poison/attack code anywhere. MoCBehavior.java line 130-131 registers the "wyvern" spec WITHOUT .hostile(), so MoCAnimal.registerGoals (lines 100-103, gated on behavior().wildHostile) adds no MeleeAttackGoal, HurtByTargetGoal, or NearestAttackableTargetGoal for it. The WYVERNPOISONING sound is registered (MoCSounds.java line 182) but grep for wyvernpoisoning/poisonPlayer/doHurtTarget in the wyvern file returns nothing — it is never played. Legacy MoCEntityWyvern.attackEntity (legacy lines 696-718: 5 dmg / 10 for type>=5, 1/3 chance Potion.poison 200t + wyvernpoisoning) and findPlayerToAttack (lines 742-760: nearest player within 10 when !tamed) have no counterpart in the port.

### M10. Crocodile grab-and-drown death-roll removed entirely
- **Area:** entities_ai — **verdict:** confirmed_partial
- **What’s missing/reduced:** The surveyor is correct. The crocodile's signature grab-and-drown death-roll is entirely absent from the port. The legacy behavior — when close to prey it mounts the victim onto itself (mountEntity), drags it underwater, resets the victim's deathTime each tick to keep it drowning, plays 'crocroll' during the roll for extra bite damage, and releases on death/escape — has no counterpart in the port. There is no tick loop, no HasCaughtPrey flag, no bite-progress state, no drown logic, and the registered CROCROLL sound is dead code (registered + in sounds.json but never invoked). Classified confirmed_partial rather than confirmed_missing only because the crocodile entity itself IS present and functions as a hostile 7-damage melee attacker with correct grunt/hurt/dying sounds and reptilehide drop — but its defining amphibious ambush/death-roll behavior is fully stripped, reducing it to a generic land predator. Severity medium: player-visible loss of a well-known signature behavior, but the mob still exists and fights.
- **Verified in port:** Port MoCEntityCrocodile.java (C:\Users\warwa\ModDev\Mo' Creatures 26.2 Multiloader\common\src\main\java\drzhark\mocreatures\entity\passive\MoCEntityCrocodile.java) is 50 lines: only createAttributes (7 ATTACK_DAMAGE), getTexture, and three sound overrides. No tick/aiStep/doHurtTarget override, no prey-grab logic. MoCBehavior.java line 104: reg("crocodile").baby().hostile() = generic melee. Grep across the whole port for CaughtPrey|Prey|biteProgress|IsBiting|Crocodile(Attack|Goal) returns ZERO crocodile matches (only unrelated BigCat/Werewolf/Snake "prey" comments). CROCROLL is registered (MoCSounds.java:47) and defined in sounds.json:160 but referenced NOWHERE else — never played. No entity/ai goal directory exists in the port. Legacy MoCEntityCrocodile.java confirms the full mechanic: getHasCaughtPrey(), entity.mountEntity(this) (line 392), ((EntityLiving)riddenByEntity).deathTime = 0 to force-drown (lines 283/589), and playSoundAtEntity(this,"crocroll",...) during the roll (line 315).

### M11. Snake venom assigned to wrong variants (cobra & coral lost venom, python wrongly venomous)
- **Area:** entities_ai — **verdict:** confirmed_partial
- **What’s missing/reduced:** CONFIRMED. The port's venom set {3,4,7,8} is off by two from legacy {3,4,5,6,7}. Both files use the same type-ID semantics (verified via matching texture switch and the legacy type comment block), so this is a genuine assignment bug, not a renumbering. Player-visible effects: (1) coral snake (type 5) and cobra (type 6) — the two iconic venomous species, cobra even legacy-flagged as a spitter — no longer poison on bite; (2) python (type 8), a large constrictor legacy deliberately leaves harmless, now poisons players. The port's own doc comment at line 84 is internally contradictory ("Cobra / coral variants (3, 4, 7, 8)" — but 3/4 are orange/green and the real cobra/coral 5/6 are excluded), suggesting a transcription error rather than intent. Correct fix: return t >= 3 && t <= 7.

Secondary reductions the surveyor noted are also real but lower impact: the legacy per-species behaviors are collapsed. Every snake in the port applies a blanket SLOWNESS "constrictor grip" (doHurtTarget line 94-96), which legacy did NOT do — legacy had no generic slow; instead type 7 rattlesnake had a rattle warning (legacy lines 461-464, 541-544) and the mod's snakes had prey/near-player behaviors. Those species-specific warning/attraction behaviors are dropped/adapted. But the core defect — the venom mask itself — is the confirmed partial regression.
- **Verified in port:** Port MoCEntitySnake.java:85-88 isVenomous() returns `t == 3 || t == 4 || t == 7 || t == 8`, used at line 98-100 (the ONLY POISON source for snakes in common/fabric/neoforge, confirmed by grep). Legacy MoCEntitySnake.java:668 attackEntity(): `getType() > 2 && getType() < 8` = venom for types {3,4,5,6,7}. Type mapping is identical in both (legacy comment lines 107-113 and port getTexture() lines 57-66: 5=coral, 6=cobra, 7=rattlesnake, 8=python). So coral(5) and cobra(6) lost venom; python(8) — legacy explicitly "non venomous" constrictor — now injects poison.

### M12. Pet Scorpion / Scorpion: wall-climbing and pet-scorpion vial transforms
- **Area:** entities_ai — **verdict:** confirmed_partial
- **What’s missing/reduced:** Both features are genuinely absent from the current port, confirming the surveyor's "partial" verdict. (1) Wall-climbing: legacy gives both the monster and pet scorpion spider-style wall climb via isOnLadder()=isCollidedHorizontally; the port overrides onClimbable on neither scorpion, so they lose their defining vertical movement. The only onClimbable in the repo is for flying insects. (2) Pet-scorpion vial transforms: MoCEntityPetScorpion has no interaction override whatsoever, so the legacy vialundead->undead(type5) morph and vialdarkness->heal+spawn-egg are both missing. This is a clear oversight rather than a deliberate adaptation, because the port already re-implemented the identical essence/vial->type-5 transform mechanic for 14 other creatures and simply skipped the scorpion; the type-5 undead texture is shipped but can never be reached. Player-visible impact is medium: no wall-climbing (movement regression on both scorpions) and no way to obtain the undead pet-scorpion variant. Fixes: add an onClimbable override to both scorpion classes (return horizontalCollision) and an ESSENCEUNDEAD mobInteract handler on MoCEntityPetScorpion setting type 5, mirroring MoCEntityWyvern/MoCEntityHorse.
- **Verified in port:** Legacy MoCEntityPetScorpion.java: isOnLadder() returns isCollidedHorizontally (L180-183) + climbing() (L185-188) = spider wall-climb; interact() handles vialundead->setType(5) undead (L466-478) and vialdarkness->heal+drop egg (L480-502). PORT: grep for isOnLadder|onClimbable|setClimbing across the whole Multiloader repo returns only MoCFlyingInsect.onClimbable (L51, insects) plus flight files (Wyvern/Bird) — NEITHER scorpion class overrides onClimbable, so both scorpions crawl on the ground only. Grep for vialundead|vialdarkness|[Vv]ial: zero matches in any scorpion file; MoCEntityPetScorpion.java (86 lines) has NO mobInteract/interact override at all (grep mobInteract|useItemOn|interactAt = no matches). The port's essence-transform pattern (setTypeMoC(5)) is wired into 14 other creatures (Wyvern L343, Horse L367, Kitty, Snake, Snail, Bird, BigCat, WWolf, etc.) but NOT the scorpion — so pet-scorpion type 5 (scorpionundead.png in getTexture case 5) is unreachable in gameplay. Baby-drop (MoCEntityScorpion.die L138-158, HAS_BABIES) and per-type sting (applyHitEffects L118-136) ARE faithfully ported.

### M13. Bear: polar bear unreachable, hind-leg-stand / panda-sit / cub-sit state machine dropped, taming over-generalized
- **Area:** entities_ai — **verdict:** confirmed_partial
- **What’s missing/reduced:** The surveyor is correct on all three sub-points. (1) Polar bear (type 4) is unreachable in survival: selectType picks 1-3 only and no biome-based type-4 assignment exists (legacy's checkSpawningBiome is entirely gone). The bearpolar.png texture case is dead code; a polar bear can only appear via /summon or NBT type=4. (2) The bear-state machine is fully dropped: no data field, no getBearState/setBearState, no standingCounter, no isMovementCeased/getCustomSpeed sit-stop, and the model has no standing (hind-leg-rear near player) or sitting (panda/cub) poses — only the walking gait. Legacy panda-seeks-sugar/reed-item-on-ground behavior is also absent. (3) Taming is over-generalized: MoCBehavior tames every bear type with sugar/sugar-cane, whereas legacy tames only pandas (type 3) with sugarlump/reed. Classified partial (not missing) because the entity still exists, spawns via biome injection, attacks, and has correct textures/sounds/on-fours model. Player-visible impact is medium: a whole spawnable variant is inaccessible, the bears' most recognizable idle animations are gone, and any bear (brown/black/polar) can be tamed contrary to design.
- **Verified in port:** Port MoCEntityBear.java (common/.../entity/passive/MoCEntityBear.java, 69 lines total) contains ONLY createAttributes, selectType, getTexture, and 3 sound overrides. Grep of entire common/fabric/neoforge Java tree for BearState|bearState|getBearState|standingCounter returns ZERO hits. selectType() (lines 32-43) picks only i<=40->1, <=80->2, else 3 — never 4. finalizeSpawn in MoCAnimal.java:465-470 just calls selectType(); no checkSpawningBiome / cold-biome / Taiga / FROZEN logic exists anywhere (grep empty). The `case 4 -> bearpolar.png` branch (line 50) is dead code, never reached. MoCModelBear.setupAnim only animates the on-fours walking gait (no standing/sitting pose branches). MoCBehavior.java:97 registers reg("bear").tame(Tame.FEED).food(SUGAR, SUGAR_CANE) — applies to ALL bear types. Legacy MoCEntityBear.interact() tames only getType()==3 (panda) with sugarlump/reed; legacy checkSpawningBiome() set type 4 in Taiga/Frozen Ocean/Frozen River/Ice Plains/Ice Mountains/TaigaHills; legacy onLivingUpdate/getBearState drove 0/1/2 states with isMovementCeased and getCustomSpeed==0 when sitting.

### M14. Special-horse amulets (bone/ghost/fairy/pegasus, empty+full) are completely inert
- **Area:** items — **verdict:** confirmed_missing
- **What’s missing/reduced:** The surveyor is correct. All 8 special-horse amulet items exist as art + registrations only, with no behavior. Empty amulets do nothing when right-clicked on the matching undead/ghost/fairy/pegasus horse (legacy consumed them via MoCEntityHorse.interact and vanished the horse into a full amulet); full amulets do not release a stored horse (legacy MoCItemAmulet.onItemRightClick); and no full amulet drops when such a horse dies (legacy MoCEntityHorse death -> MoCTools.dropAmulet). The horse entity itself DOES model the special-horse types (isUndead/isGhost/isMagicHorse, pegasus/fairy type ids at lines 110-194), so the entities exist — only the amulet capture/transport/recovery/death-drop mechanic tied specifically to these items is absent. The generic PETAMULET provides a parallel capture-any-tamed-creature flow, so it is not a total loss of capture functionality for special horses, but it is a different item and does not reproduce the special-amulet crafting-tier gating, the type-preserving full-amulet-by-coat, or the death-drop. Classifying as 'missing' (not 'adapted') because the 8 named items are genuinely non-functional and no code deliberately routes their role through PETAMULET. Severity medium: player-facing (8 craftable/obtainable items are dead, and the death-drop safety net for losing a rare special horse is gone), but core capture of special horses is still achievable via PETAMULET.
- **Verified in port:** In the port the 8 items appear ONLY as plain registrations in MoCItems.java:120-135 (AMULETBONE/FULL, AMULETFAIRY/FULL, AMULETGHOST/FULL, AMULETPEGASUS/FULL — every one `new Item(new Item.Properties())`, no custom Item class) plus texture/model/lang assets. Grep for AMULETBONE|GHOST|FAIRY|PEGASUS across all .java in common/fabric/neoforge returns ZERO hits outside MoCItems.java. MoCEntityHorse.java (common\...\entity\passive\MoCEntityHorse.java) has NO amulet/vanish/dropAmulet references (grep empty) — so no capture-on-interact and no full-amulet death-drop. No loot table under common\...\resources\data references 'amulet'. Legacy behavior confirmed present: MoCTools.java:1955 dropAmulet + :2010-2059 return amulet(full) by horse getType(), MoCItemAmulet release, registered via MoCItemAmulet in MoCreatures.java:591-598. The generic PETAMULET (item\MoCAmuletItem.java) captures any tamed IMoCEntity — including a tamed special horse — but is a SEPARATE item that never consumes these 8 amulets, never emits their 'full' variants, and provides no death-drop; the 8 special-horse amulet items themselves are wired to nothing.

### M15. Thrown mocegg hatches a random species instead of a per-subtype egg
- **Area:** items — **verdict:** confirmed_partial
- **What’s missing/reduced:** CONFIRMED PARTIAL. Legacy had a single subtyped egg item (fishyegg/moc_egg, setHasSubtypes) whose item damage deterministically selected the hatchling species AND variant; the port collapses this into one non-subtyped 'mocegg' that always hatches a UNIFORMLY RANDOM species among 6 (ostrich/turtle/snake/komodo/scorpion/wyvern). Reductions vs legacy: (1) no determinism — you cannot choose what hatches; (2) fish (types 1-10) and shark (11) egg hatching entirely absent; (3) per-variant selection within a species (e.g. snake sub-types 21-28, scorpion 41-45, wyvern 50-54) gone; (4) special 'stolen ostrich egg' (type 31) that hatched a pre-tamed ostrich is gone; (5) hatch gating (torch-proximity for land eggs, water for aquatics, ostrich pop-cap of 20) not reproduced — the port egg just rests on ground and hatches on a 1600-tick timer. The surveyor's 'partial / medium' classification is correct. Note: the port's turtle-inclusion (type 2) is a small addition, but does not restore the lost typed-item mechanic.
- **Verified in port:** Port has exactly ONE thrown egg: MoCItems.MOCEGG (registry/MoCItems.java:285-286, a MoCThrownEggItem, lang "Spoiled Egg"). MoCThrownEgg.onHit (entity/projectile/MoCThrownEgg.java:42) hardcodes egg.setTypeMoC(1 + this.random.nextInt(6)) — random among 6 species only; the item carries no subtype (MoCThrownEggItem.use passes no metadata). Port MoCEntityEgg (entity/passive/MoCEntityEgg.java:25-30) supports only TYPE_OSTRICH/TURTLE/SNAKE/KOMODO/SCORPION/WYVERN, no fish/shark. Legacy MoCItemEgg (legacy .../item/MoCItemEgg.java:15,30,35) is setHasSubtypes(true) and does new MoCEntityEgg(world, itemstack.getItemDamage()) so metadata deterministically chooses hatchling across eggType 1-54 (fishy 1-10, shark 11, snakes 21-28, ostrich 30/31/32, komodo 33, scorpions 41-45, wyverns 50-54) per legacy MoCEntityEgg.onUpdate. Grep of MoCItems.java found no fishyegg/typed-egg items — the other *_spawn_egg entries are vanilla SpawnEggItem creative spawners, not the subtype system.

### M16. Wyvern Lair return position is non-persistent (in-memory map, lost on restart)
- **Area:** meta_critic — **verdict:** confirmed_partial
- **What’s missing/reduced:** CONFIRMED PARTIAL (not "missing" — the return-teleport feature works within a single server session, which is why I downgrade from missing). The regression: the departure point lives only in a static JVM ConcurrentHashMap. If the server restarts (or the JVM otherwise reloads the class) while the player is inside the Lair, the map is empty on return, `RETURN.remove()` yields null, and the fallback at MoCStaffPortalItem.java lines 75-77 teleports the player to the Overworld MOTION_BLOCKING_NO_LEAVES heightmap column at the player's *Lair* X/Z — an essentially arbitrary Overworld spot, not their real departure location. Legacy avoided this by storing the origin in the staff item's NBT, which persisted across restarts and even followed the item. Trigger window is narrow (restart/crash specifically while a player is mid-Lair), and normal same-session round trips are unaffected, hence medium rather than high severity. Note: keying by UUID in a static map also means a second staff cannot hold an independent origin and the value is not per-item as in legacy, but the player-visible impact of that is minor compared to the restart-loss. Fix would be to persist the origin either on the ItemStack (DataComponent, matching legacy semantics) or in a level SavedData / player persistent data.
- **Verified in port:** common/.../item/MoCStaffPortalItem.java line 35 declares `private static final java.util.Map<java.util.UUID, Vec3> RETURN = new java.util.concurrent.ConcurrentHashMap<>()`. It is written only at line 55 (`RETURN.put(serverPlayer.getUUID(), serverPlayer.position())` when travelling TO the Lair) and read/removed only at line 71 (`Vec3 back = RETURN.remove(serverPlayer.getUUID())` when returning). Grep across common+fabric+neoforge for `RETURN.`, `portalPos`, `getPersistentData`, `SavedData`, `PlayerEvent`, `onLogout`, `onDisconnect` returns only those two lines plus unrelated entity/item NBT (fishbowl, amulet, horse, etc.) — no persistence hook feeds this map. Legacy ItemStaffPortal.java (lines 71-76, 202-216) persisted portalPosX/Y/Z/Dimension into the item's NBT via writeToNBT/readFromNBT, surviving restart.

### M17. Staff of Portal missing "blocked while mounted/being ridden" guard
- **Area:** meta_critic — **verdict:** confirmed_missing
- **What’s missing/reduced:** Legacy ItemStaffPortal.onItemUse no-op'd (`if (thePlayer.ridingEntity != null || thePlayer.riddenByEntity != null) return false;`) to avoid stranding mounts on a dimension change. The port's MoCStaffPortalItem omits this: only the ServerPlayer is moved via TeleportTransition, so a mount/steed (or a rider on the player) is left behind in the origin dimension while the player is teleported out from under it — potentially mid-flight over the Lair void. Notably the guard IS present in the closely-related MoCStaffTeleportItem, showing the omission is an oversight rather than a deliberate design choice. Fix is a one-line guard at the top of use(): `if (player.isPassenger() || player.isVehicle()) return InteractionResult.PASS;` mirroring the teleport staff. Severity medium: player-visible mount loss/stranding, but only triggers when actively riding or being ridden while using the portal staff.
- **Verified in port:** common/src/main/java/drzhark/mocreatures/item/MoCStaffPortalItem.java use() (lines 42-86) has no riding check — grep for isPassenger|isVehicle|getVehicle|getPassengers|startRiding|stopRiding|ejectPassengers in that file returned "No matches found". use() proceeds straight into dimension-change logic and calls serverPlayer.teleport(new TeleportTransition(...)) unconditionally at line 80. By contrast the sibling MoCStaffTeleportItem.java (lines 29-31) DOES guard: `if (player.isPassenger() || player.isVehicle()) return InteractionResult.PASS;` with javadoc "Does nothing while riding or being ridden." Only the portal staff is missing it. No loader-specific override exists (StaffPortal referenced only in MoCItems.java registration and MoCLairSky.java).

### M18. despawnVanilla subsystem (auto-culling distant vanilla farm animals)
- **Area:** meta_critic — **verdict:** confirmed_missing
- **What’s missing/reduced:** Genuinely absent, not renamed. The port has no tick handler, no MoCTools.despawnVanillaAnimals equivalent, and no DespawnVanilla/despawnTickRate config key. MoCMobCap.java is a DIFFERENT feature (a self-cap on Mo'Creatures' own untamed non-persistent entities) that the surveyor correctly did not conflate with this. Player-visible impact is indirect but real: on a Mo'Creatures world, vanilla passive mobs (cows/sheep/pigs/chickens/squid/wolves) are no longer auto-culled at distance, so they accumulate in loaded chunks and dilute the mob-cap headroom available to MoC natural spawns, reducing MoC creature density relative to the original mod's on-by-default ecological balance. Not a crash or hard feature loss — hence medium, not high. To restore faithfully: add a SERVER_LEVEL_POST (or existing MoCMobCap sweep) pass that discards non-tamed vanilla Cow/Sheep/Pig/Chicken/Squid/Wolf beyond ~128 blocks from any player, gated on a new `despawnVanilla` (default true) config key with a `despawnTickRate` interval.
- **Verified in port:** Port greps all empty: `despawn`/`despawnVanilla`/`removeVanilla` = 0 Java hits (only an unrelated doc-comment in common/.../entity/passive/MoCEntityFishBowl.java:29). The only file in common/.../spawn/ is MoCMobCap.java, which I read fully (169 lines): its loop does `if (!(e instanceof IMoCEntity moc)) continue;` (lines 80-83), so it counts/discards ONLY Mo'Creatures entities against MoC config caps (maxAnimals/maxMobs/etc.) — it never touches vanilla Cow/Sheep/Pig/Chicken/Squid/Wolf. A follow-up grep for `Cow|Sheep|Chicken.class|EntityType.COW|...|instanceof Animal` across the whole port = 0 matches. MoCConfig.java has `modifyVanillaSpawns` (line 66, a spawn-weight override) but no DespawnVanilla / despawnTickRate key. Legacy source of truth: MoCServerTickHandler.java:57-62 → MoCTools.despawnVanillaAnimals (MoCTools.java:1417) + entityDespawnCheck (line 1432) cull vanilla EntityCow/Sheep/Pig/Chicken/Squid/Wolf beyond 128 blocks; config DespawnVanilla defaults true (MoCProxy.java:1175). None of this is reimplemented anywhere in the port.

## LOW impact (18)

### L1. Wyvern Lair tree simplified from branching big-tree to straight-trunk blob tree
- **Area:** blocks_worldgen_dim — **verdict:** confirmed_partial
- **What’s missing/reduced:** The claimed gap is real and correctly characterized as partial/low. Legacy: 90% of trees were WorldGenShrub(3,0) (tiny shrubs) and 10% were the tall custom MoCWorldGenBigTree (double trunk, sloped branches, foliage clusters, height up to 30), at treesPerChunk=4. Port: a single uniform straight-trunk blob tree (max height ~7, radius-2 blob) at count 3 per chunk, with NO big-tree variety and NO shrub variety. So the port loses (a) the occasional tall ~30-block branching landmark tree and (b) the dominant-shrub understory, replacing the whole mix with one medium blob oak. It is a genuine reduction, not an equivalent-under-a-different-name reimplementation. Mitigating: blocks/leaves are correct (log_wyvern_lair/leaves_wyvern_lair), and mushrooms survive (biome/wyvern_lair.json still lists minecraft:brown_mushroom_normal and red_mushroom_normal), so only the tree silhouette/variety is affected. Purely worldgen aesthetics, no gameplay/spawn impact. Could be closed by adding a fancy/large-trunk tree variant (e.g. cherry_trunk_placer or a custom large trunk with random_spread foliage) plus a shrub feature and weighting them ~10/90 to match legacy.
- **Verified in port:** Port has exactly ONE tree feature: common/src/main/resources/data/mocreatures/worldgen/configured_feature/wyvern_lair_tree.json is a vanilla minecraft:tree with straight_trunk_placer (base_height 5, height_rand_a 2, height_rand_b 0 -> max ~7 blocks) and blob_foliage_placer (radius 2, height 3), using mocreatures:log_wyvern_lair / leaves_wyvern_lair. placed_feature/wyvern_lair_tree.json uses count:3 per chunk. A grep of the whole data/mocreatures worldgen tree for fancy_trunk/large_oak/forking_trunk/shrub/big_tree returned NO matches, and the configured_feature+placed_feature dirs contain only wyvern_lair_grass, wyvern_lair_tree, wyvern_portal (no second tree variety, no shrub feature). Legacy BiomeGenWyvernLair.getRandomWorldGenForTrees (line 55) returns rnd.nextInt(10)==0 ? new MoCWorldGenBigTree(false,mocLog,0,mocLeaf,0,2,30,10) : new WorldGenShrub(3,0), with treesPerChunk=4 and mushroomsPerChunk=20; MoCWorldGenBigTree.java is confirmed 549 lines of branching/sloped-foliage logic up to height 30.

### L2. Grass block 'depthsuspend' floating end-particle ambient effect not ported
- **Area:** blocks_worldgen_dim — **verdict:** confirmed_missing
- **What’s missing/reduced:** Purely cosmetic ambient effect. In 26.2 this would be an animateTick(BlockState, Level, BlockPos, RandomSource) override on MoCLairGrassBlock emitting e.g. ParticleTypes.PORTAL (the modern analogue of the legacy "depthsuspend"/End-portal particle) at pos.getY()+1.1 roughly 1/75 random ticks. The core gameplay logic (grass spread/revert) is faithfully ported; only the floating sparkle over lair ground is absent. Note the same particle also appeared on the legacy MoCBlockFarm (farm block), so if that block was ported its ambient particle is likely absent too.
- **Verified in port:** Legacy MoCBlockGrass.randomDisplayTick (legacy-source/.../block/MoCBlockGrass.java:97-105) spawns the "depthsuspend" particle at y+1.1F ~1/75 ticks. The port's MoCLairGrassBlock.java (common/.../block/MoCLairGrassBlock.java) implements ONLY randomTick(BlockState,ServerLevel,BlockPos,RandomSource) for spread/revert — no animateTick override and no particle spawn. Grep for depthsuspend|animateTick|randomDisplayTick|spawnParticle|addParticle across the whole port (common/fabric/neoforge) returned no matches in any block class; the sole particle hit was MoCEntityHorse.java, an unrelated entity. Grep scoped to the block package returned "No matches found".

### L3. Leaf block has no sapling drop or shears behaviour
- **Area:** blocks_worldgen_dim — **verdict:** confirmed_partial
- **What’s missing/reduced:** Confirmed partial/reduced. Legacy MoCBlockLeaf extended BlockLeavesBase and had full leaf semantics: 1-in-20 chance to drop a vanilla sapling on break (idDropped -> Block.sapling), shear-to-collect the leaf block itself (harvestBlock with shears check), plus leaf-decay ticking (updateTick/removeLeaves) and adjacent-sapling meta flagging (onBlockRemoval). The port replaces this with a plain Block that has none of these: no sapling drop, no shears interaction, no decay, no random ticks. There are no loot tables anywhere in the port, so both leaf blocks simply drop themselves via their default BlockItem. Player-visible impact is minor: (a) there is no lair sapling item in the port (legacy dropped vanilla Block.sapling, an arguably-buggy cross-drop of an oak sapling from a lair tree), so no lair-specific yield is lost; (b) leaf decay is cosmetic in a small structural dimension. The genuine loss is shear-to-collect the decorative leaf block, which is a low-severity convenience regression. Not a deliberate documented adaptation — just a simplified reimplementation as an inert decorative block.
- **Verified in port:** Port: MoCBlocks.java L33-34 (LEAVES_WYVERN_LAIR) and L48-49 (LEAVES_OGRE_LAIR) are plain net.minecraft.world.level.block.Block with Properties.of().strength(0.2F).noOcclusion() — no LeavesBlock, no custom subclass, no randomTicks. Glob for **/loot_table/**/*.json across the whole port returned NO files (zero loot tables exist). Grep for Shears|getDrops|onBlockBreak|BlockEvent|playerDestroy across the entire port returned no matches. Grep for sapling/Sapling in MoCItems.java: no matches (no lair sapling item exists). Legacy MoCBlockLeaf.java: idDropped() returns Block.sapling.blockID (L216), harvestBlock() drops mocLeaf when shears held (L223-227).

### L4. Special-horse StarFX trails (sprint / wing-flap / pegasus / unicorn)
- **Area:** client_render_fx — **verdict:** confirmed_partial
- **What’s missing/reduced:** The gap is real. The FX_STAR particle itself is fully ported and registered (MoCParticles.FX_STAR + MoCParticleFactories provider), but it is emitted only by the amulet capture burst (MoCAmuletItem.java:86). The in-world magic-horse star trail — legacy StarFX() fired while a pegasus/unicorn/fairy horse is airborne, sprinting (sprintCounter trail), or flapping (wingFlapCounter sparkles) — has no counterpart in the port's MoCEntityHorse tick, which only emits vanilla FLAME for nightmares. So flying/sprinting magic horses produce no trailing sparkle. Classified partial rather than missing because the underlying particle and emission plumbing exist and merely need to be wired into the horse tick under the isFlyer()/isMagicHorse() gates that are already present. Severity is low: purely cosmetic — the horses still fly, sprint, animate wing-flap, and read as magical via their coats, models, and crystal armour; only the ambient sparkle trail is absent.
- **Verified in port:** Grep for StarFX|FX_STAR|starFX across the entire port returns only 4 hits: MoCParticleFactories.java (particle provider registration), MoCParticles.java (FX_STAR type declaration), and MoCAmuletItem.java:86 (capture burst). Zero hits in MoCEntityHorse.java. Grep of the port MoCEntityHorse.java for addParticle|sendParticles returns only two lines, both vanilla ParticleTypes.FLAME: line 269 (nightmare type-38 smoulder) and line 635 (whip-crack nightmare burst). Grep for sprintCounter|wingFlapCounter|StarFX in the horse file yields a single hit at line 616 — a Javadoc COMMENT referencing legacy sprintCounter, not live code. The port has isFlyer()/isMagicHorse()/isUndead()/isFlyingHorse() gating (lines 111-195) but never calls any star emitter. Legacy MoCEntityHorse.java by contrast calls StarFX() from three tick contexts: airborne flyer (line 2214), sprint trail (line 2506, every 2 ticks while sprintCounter<150), and wing-flap (line 2521, every 5 ticks), routing through MoCreatures.proxy.StarFX -> MoCEntityFXStar coloured sparkles.

### L5. Ghost-horse idle bob animation
- **Area:** client_render_fx — **verdict:** confirmed_missing
- **What’s missing/reduced:** The surveyor is correct. Ghost horses (types 21/22) in the port render statically like a normal horse because no client-side code applies the legacy adjustHeight(-0.3F + tFloat()/5F) vertical translate. The entity retains isGhost() for gameplay logic, but the render pipeline (MoCHorseRenderer -> MoCMobRenderer) never consults it, and the render state carries no ghost/bob flag. To restore the spectral float, the port would need to expose a ghost flag on MoCEntityRenderState in extractRenderState and apply a sin-driven Y translate in a scale()/pose override for the horse renderer. Player-visible impact is minor/cosmetic (loss of the floating spectral feel on two rare ghost coats), hence low severity.
- **Verified in port:** Legacy MoCRenderNewHorse.preRenderCallback (legacy-source\...\client\renderer\entity\MoCRenderNewHorse.java lines 34-37) calls adjustHeight(entityhorse, -0.3F + entityhorse.tFloat()/5F) when isGhost(), giving a per-frame vertical bob. In the port, MoCHorseRenderer.java (common\...\client\renderer\MoCHorseRenderer.java, 18 lines) only sets shadow radius 0.7F and delegates to MoCMobRenderer. MoCMobRenderer.scale() (lines 72-78) applies ONLY baby scaling; extractRenderState() (lines 36-70) sets no ghost/float/bob field; MoCEntityRenderState has no ghost or bob field (grep for Ghost/bob/floatY/translate returned no matches). Grep across the entire port (common+fabric+neoforge) for tFloat|adjustHeight|floatWabble|spectral and for isGhost in the client package returned no client-side hits — every tFloat-style match is an unrelated random nextFloat(). isGhost() exists only in MoCEntityHorse.java (lines 122-126) as server-side type logic; no renderer or model reads it.

### L6. Legacy Jump / Dive / Dismount keybinds and their C2S packets absent
- **Area:** config_cmd_gui_hud — **verdict:** confirmed_partial
- **What’s missing/reduced:** Legacy MoCKeyHandler.java registers Jump (=vanilla keyBindJump) and Dismount (F) keybinds (the third, "Dive", is declared but commented out of the registration array, so legacy shipped Jump+Dismount+GUI only), each firing IMoCreature.makeEntityJump()/dismountEntity() plus a C2S packet. In the port: (1) DISMOUNT — no dedicated key/packet; players rely on the vanilla dismount key (shift), an acceptable adaptation. (2) DIVE — reimplemented as look-pitch steering in MoCAquatic.getRiddenInput, so submerging still works without a key; this is a deliberate 26.2 adaptation. (3) JUMP CHARGE — the genuinely reduced piece: legacy makeEntityJump() gave rideable land/flying mounts an explicit rider-triggered jump/leap. The port's MoCAnimal ridden movement has no jump-charge, no PlayerRideableJumping implementation, and no vertical control, so land mounts cannot be commanded to jump/leap on demand the way legacy allowed. Overall the keybinds+packets are confirmed absent, but the player-facing effects are mostly covered by adapted mechanisms; only the explicit mount-jump command is a real functional reduction, hence low severity.
- **Verified in port:** Port keybind registry MoCKeyMappings.java (common/.../client/) registers exactly ONE mapping: key.mocreatures.settings (F9). No key.mocreatures.jump/dive/dismount exists. Grep across the whole live root for makeEntityJump|makeEntityDive|dismountEntity|sendEntityJump/Dive/Dismount|PlayerRideableJumping|onPlayerJump|handleStartJump returned NO matches. Network layer is only two files: MoCNetwork.java registers a single C2S receiver (MoCNamePayload) — no jump/dive/dismount payloads (legacy packet IDs 21/24/25). Diving is instead folded into MoCAquatic.getRiddenInput (line 136: vertical = -controller.getXRot()*0.02D*forward) so aquatic mounts submerge by looking down; MoCAnimal.getRiddenInput (line 310-317) has NO vertical/jump component. The only stopRiding in MoCAnimal (line 146) is the entity dismounting itself, not a player force-dismount.

### L7. displayPetIcons (pet emote/status icons) HUD element
- **Area:** config_cmd_gui_hud — **verdict:** confirmed_missing
- **What’s missing/reduced:** Genuinely absent. The displayPetIcons config flag + its GUI/command toggle are dead: no code reads it to render anything. In legacy this flag drove the KITTY emote/mood billboard specifically — getEmoticon() mapped the cat's FSM mood to one of ~19 emoticon textures drawn as a 32x32 quad above the cat (kitty-only; the same flag was read but unused in the base animal/bigcat/shark renderers). The port has no getIsEmo/getEmoticon on MoCEntityKitty and no renderer draws emote icons, so the whole emote-icon subsystem is missing even though the emoticon PNGs were copied into resources. Two refinements to the surveyor's framing: (1) legacy drew this as an in-world billboard above the pet, not a crosshair-HUD text line; (2) it was a kitty-specific mood indicator, not a generic all-pets readout — so wiring it into MoCPetHud would not faithfully reproduce legacy behavior; it belongs in the kitty entity renderer keyed off a revived emote/mood state. Player-visible impact is small (cosmetic cat mood emoji), hence low severity.
- **Verified in port:** Port MoCPetHud.java line 51 gates only on displayPetName/displayPetHealth; displayPetIcons is read only in MoCConfig/MoCSettingsScreen/MoCCommand (config plumbing), never in any render path. Grep for `emoticon|getIsEmo|setIsEmo|getEmoticon` across common/fabric/neoforge Java returns ZERO hits; port MoCEntityKitty.java has no emote state. Legacy MoCEntityKitty.java (getIsEmo l.157, getEmoticon l.432-494) + MoCRenderKitty.doRenderLiving (l.58-90) billboarded ~19 emoticonN.png textures above the cat when `displayPetIcons && getIsEmo()`. The emoticonN.png assets ARE shipped in port (textures/misc/, copied from the 12.0.5 jar) but are orphaned — nothing references them.

### L8. BigCat: hunger-gated hunting, pride grouping, and owner's-enemy targeting simplified
- **Area:** entities_ai — **verdict:** confirmed_partial
- **What’s missing/reduced:** The claim holds. Core combat is present (leap impulse in doHurtTarget lines 92-109; medallion death-drop lines 116-122; medallion taming + whip/sit + heal-on-feed all inherited from MoCAnimal.mobInteract). But four legacy nuances are genuinely absent: (1) HUNGER-GATED HUNTING — legacy findPlayerToAttack only returns a target when getIsHungry() and then setHungry(false), re-arming via onLivingUpdate rand.nextInt(200); the port hunts continuously (generic always-on target goal). (2) PRIDE GROUPING — legacy checkNearBigKitties merges lioness type 2->1 near another bigcat, and getClosestTarget refuses same-type/tamed/type-7/stronger cats; the port has none of this filtering. (3) OWNER'S-ENEMY TARGETING — legacy getMastersEnemy makes the cat attack whatever is targeting its master, though note this is gated on the legacy `roper` (lasso) field being non-null, i.e. it fires for a roped cat rather than any tamed pet; the port has no equivalent OwnerHurt goal at all. (4) GROUND-FOOD SELF-HEAL + SLOW REGEN — legacy onLivingUpdate seeks dropped porkRaw/fishRaw within 12 blocks, eats to full-heal and (baby) age up, and separately regens 1 HP on rand.nextInt(300); the port only heals via right-click feeding in mobInteract and has no over-time regen or ground-eating. Classify partial. Severity low: these are subtle AI-flavor behaviors, not player-obvious functionality — an always-hungry wild lion looks the same as a hunger-gated one to a player, and the missing self-heal/owner-defense are minor pet-quality regressions rather than broken features.
- **Verified in port:** Port MoCEntityBigCat.java (common\...\entity\passive\MoCEntityBigCat.java) is 123 lines: it implements only selectType, getTexture, sounds, doHurtTarget (leap), and dropCustomDeathLoot (medallion). It has NO getIsHungry/setHungry, NO checkNearBigKitties, NO getMastersEnemy, NO onLivingUpdate ground-food/heal loop. Grep across the whole port for `getIsHungry|setHungry|checkNearBigKitties|getMastersEnemy|getClosestItem` = 0 hits; grep for `OwnerHurtByTargetGoal|OwnerHurtTargetGoal|DefendOwner|MastersEnemy` = 0 hits. Hunting comes entirely from MoCAnimal.registerGoals (lines 100-106): an always-on NearestAttackableTargetGoal with predicate `!getIsTamed() && getIsAdult()` plus HurtByTargetGoal — no hunger gate, no pride filtering, no OwnerHurt/defend-owner target goal. The only `Hungry` hits are in MoCEntityKitty.java (an unrelated kitty mood FSM, STATE_HUNGRY).

### L9. Deer flee-from-predator + running jump behavior removed
- **Area:** entities_ai — **verdict:** confirmed_partial
- **What’s missing/reduced:** Confirmed reduced, not fully missing. The deer still gets an escape reaction via the inherited vanilla PanicGoal in MoCAnimal (line 91), so it will bolt after being struck. But the legacy skittish-prey behavior is genuinely absent: (1) no getBoogey(10) predator/player-proximity detection to flee BEFORE being hit; (2) no setMySpeed(true) speed-doubling (legacy raises base 1.3-1.9 to 2.6-3.8 while fleeing); (3) no running-jump/leap (updateEntityActionState motionY=0.6 while sprinting). Also the legacy type-3 fawn->random-adult growth in onLivingUpdate (setEdad/setAdult after age 130) is not ported. Net player-visible effect: deer behave like generic passive animals that only react once hurt, rather than nervous prey that dash and leap away from an approaching predator/player. Severity low because a functional panic/flee reaction does exist; only the distinctive proactive, faster, leaping flavor is lost.
- **Verified in port:** Port MoCEntityDeer.java (C:\Users\warwa\ModDev\Mo' Creatures 26.2 Multiloader\common\src\main\java\drzhark\mocreatures\entity\passive\MoCEntityDeer.java) is 70 lines: constructor, selectType, getTexture, and sound overrides only. NO registerGoals/tick/aiStep override, so it inherits only MoCAnimal.registerGoals which at line 91 adds vanilla PanicGoal(this,1.5D) (fires after being hurt, not on predator proximity). Port-wide grep for getBoogey|runLikeHell|setMySpeed|FleeFromPredator|AvoidEntityGoal|runningJump = No files found. Also grep for AvoidEntityGoal|FleeSunGoal|new AvoidEntity across entire Multiloader tree = No files found. Legacy MoCEntityDeer.onLivingUpdate calls getBoogey(10D)->setMySpeed(true) (doubles speed) + runLikeHell(entityliving), and updateEntityActionState sets motionY=0.6 (running leap) while sprinting — none of these exist in the port.

### L10. Bird two-stage seed taming and flee-to-treetop AI simplified
- **Area:** entities_ai — **verdict:** confirmed_partial
- **What’s missing/reduced:** The surveyor is correct. Three distinct reductions vs legacy: (1) Taming is simplified from the legacy two-stage flow (bird pecks scattered seeds off the ground -> becomes preTamed -> then hand-feeding seeds tames it, per onLivingUpdate + interact) to a single instant empty-hand grab (Tame.PICKUP -> setTamed(true) + startRiding in MoCAnimal.interactAt). The preTamed data field and all seed-eating pickup code are entirely absent. (2) Predator flee AI is gone: legacy FlyToNextTree/FindTreeTop pathed the bird to the nearest tree-top canopy when a predator (getBoogey) was in sight; the port has no fleeing state at all, only vanilla WaterAvoidingRandomFlyingGoal wandering. (3) Hurt/death sounds are silent: legacy played birdhurt/birddying; port returns null for both and never registers those sounds. Per-type living/ambient sounds ARE faithfully preserved. All low player-visible impact: birds are still tameable, rideable, and make idle chirps; only the taming ritual, predator avoidance, and combat SFX are lost.
- **Verified in port:** Port MoCEntityBird.java (common/.../entity/passive/MoCEntityBird.java): registerGoals() only adds FloatGoal + WaterAvoidingRandomFlyingGoal(1.0) — no fleeing logic. getHurtSound() and getDeathSound() both `return null` (lines 111-118). getAmbientSound() correctly returns the 6 per-type BIRD* sounds. MoCBehavior.java line 101: reg("bird").tame(Tame.PICKUP).food/heal(WHEAT_SEEDS) — tame mode is PICKUP. MoCAnimal.java lines 143-160: PICKUP + empty hand => setTamed(true)+startRiding immediately (seeds used only via isMyHealFood, not as a tame gate). Grep across whole repo: "PreTamed"/"setPreTamed"/"getPreTamed" = 0 hits; "FindTreeTop"/"FlyToNextTree"/bird "fleeing" = 0 hits (the 4 fleeing hits are horse/golem/ogre/rock). Grep "birdhurt|birddying" (case-insensitive) across whole repo = 0 hits; MoCSounds.java registers only birdblack/blue/green/red/white/yellow. Legacy MoCEntityBird.java has entityInit dataWatcher(22) preTamed, onLivingUpdate eats ground seeds via getClosestItem->setPreTamed(true), interact() feeds seeds to tame via MoCTools.tameWithName, FlyToNextTree()/FindTreeTop() flee-to-treetop from getBoogey, and getHurtSound()="birdhurt"/getDeathSound()="birddying".

### L11. Turtle upside-down flip-when-attacked behavior missing
- **Area:** entities_ai — **verdict:** confirmed_partial
- **What’s missing/reduced:** The turtle entity IS ported, but its shell-defense behavior is reduced to passive damage-halving in hurtServer. The legacy mod's entire flip/hide reaction system is absent: (1) when attacked the turtle had a random chance (1/3 normally, 1/10 while hiding) to flip onto its back (isUpsideDown), during which it could not move (isMovementCeased); (2) it would then self-right after struggling (onUpdate swing/flopcounter animation, ending with a chickenplop sound and setIsUpsideDown(false)); (3) a tamed upside-down turtle could be righted by the player right-clicking it (interact -> flipflop(false)); (4) an isHiding state that halted pathing and played turtlehissing when a threat (getBoogey) was near. None of these exist in the port — no entity-data flags, no flipflop, no interact override, no model handling. The port substitutes a simpler always-on damage reduction. Player-visible impact is low (a cosmetic/flavor reaction and a minor tamed-turtle interaction), but the surveyor is correct that the visible flip mechanic is gone. Classified partial (not missing) because the turtle + a shell-toughness mechanic are present; the flip sub-behaviors are the reduced part.
- **Verified in port:** Read full port MoCEntityTurtle.java (78 lines, C:\Users\warwa\ModDev\Mo' Creatures 26.2 Multiloader\common\src\main\java\drzhark\mocreatures\entity\passive\MoCEntityTurtle.java): only createAttributes, getTexture, sound getters, hurtServer (halves non-armor-bypassing damage), and tick (egg-laying). No isUpsideDown/isHiding entity data, no flipflop, no interact-to-right, no self-righting swing/flopcounter, no getFlipDirection, no UpsideDown NBT. Grep across entire port for flip/upsideDown/isUpsideDown = 0 turtle hits (only /moc "flip a flag" doc, golem cube-flip, golem state-flip). Grep Hiding/flop/twist/Swinging turtle-specific = none (Swinging hits are vanilla swingProgress in unrelated models). Port MoCModelTurtle.java setupAnim only does leg/tail walk gait + head look — no upside-down body rotation. Legacy MoCEntityTurtle.java (C:\Users\warwa\ModDev\Mo' Creatures\_mocreatures_aside\legacy-source\src\drzhark\mocreatures\entity\passive\MoCEntityTurtle.java) has dataWatcher slot 23 isUpsideDown, flipflop(boolean), attackEntityFrom rand-chance flip, onUpdate self-righting via setSwinging/flopcounter, interact() flips tamed turtle upright, getFlipDirection twist, and UpsideDown NBT read/write.

### L12. Werewolf: gold-weapon weakness and per-form tool drops dropped; timber-wolf coat collapsed
- **Area:** entities_ai — **verdict:** confirmed_partial
- **What’s missing/reduced:** The surveyor is correct on all three sub-claims (plus the hunched leap), and I could not find any of them under a renamed method/class after searching the whole port. What IS present and faithful: day/night transform, fire-werewolf ignite (applyHitEffects type 4), HP/atk/speed swap (40/7/0.38 night vs 15/1/0.25 day), and a genuine dual human/wolf model renderer (MoCWerewolfRenderer swaps MoCModelWereHuman vs MoCModelWerewolf) — the port even improved on legacy with per-type human skins. What is genuinely reduced/missing: (1) gold-weapon vulnerability entirely absent — no damage-scaling override anywhere; (2) the per-form tool loot table replaced by a single golden-apple drop, so wood/iron/stone tool drops are gone; (3) the type-3 timber coat renders as the brown coat because wolftimber.png was never ported despite existing in the authentic 12.0.5 assets (this one is a shippable-asset omission, easily fixable); (4) hunched charge/leap AI and its speed boost removed. Correctly classified partial. Severity is low because none is game-breaking, though the timber-coat regression borders on low-medium since one of four visible coat variants is lost and the art already exists in the source jar.
- **Verified in port:** Port MoCEntityWerewolf.java (common/.../entity/monster/MoCEntityWerewolf.java, 164 lines) has NO hurt()/attackEntityFrom override — grep for GOLDEN_HOE|GOLDEN_SHOVEL|GOLDEN_PICKAXE|GOLDEN_AXE|GOLDEN_SWORD across common+fabric+neoforge returns 0 files; base MoCMob also has none. Legacy attackEntityFrom (lines 181-215) gave gold hoe/shovel/pick/axe/sword 6/7/8/9/10 dmg — gone. Loot: port uses data-driven MoCBehavior.java line 161 `reg("werewolf").drop(v(Items.GOLDEN_APPLE), 0, 1, 0.2F)` only — no tools, no per-form split; legacy getDropItemId+onDeath (lines 260-317, 374-398) dropped 2 tools/kill (wood in human form, iron+stone in wolf form). Texture: port getTexture() (lines 93-97) has cases 1(black)/4(fire)/default(brown) and NO case 3, so type 3 collapses to brownwerewolf.png; textures/models/ dir contains only black/brown/firewerewolf1-3 — Glob for *werewolf*.png confirms no timber file. Legacy getTexture case 3 = wolftimber.png. Original 12.0.5 jar DOES ship wolftimber.png (40953 bytes), so timber coat is a copy/wiring omission, NOT asset-blocked. Also confirmed absent: hunched leap/getMoveSpeed charge (grep hunched|getMoveSpeed|leap|charge in port entity = 0 matches).

### L13. Fire/Cave Ogre daylight-burn and depth-gated cave-ogre spawn
- **Area:** entities_ai — **verdict:** confirmed_partial
- **What’s missing/reduced:** Both legacy behaviors are genuinely absent from the port; the gap is real, not a mislabel. (1) Daylight sun-damage: legacy fire/cave ogres (type>2) lost 5 HP per qualifying tick in bright daylight under open sky. The port has a general burnsInDaylight() framework in MoCMob but MoCEntityOgre never overrides it, so ogres take zero daylight penalty. Note the framework ignites rather than direct-damages, and fire ogres are fireImmune() in the port, so even wiring it up wouldn't reproduce legacy (legacy used direct health subtraction that bypassed fire immunity) — this is a genuine behavioral regression, not a clean adaptation. (2) Cave-ogre depth gating: legacy only rolled a blue cave ogre when canCaveOgreSpawn() (below Y50 AND no sky access); the port rolls cave ogres on flat 50% random anywhere, so blue cave ogres can now spawn on the surface in daylight. Player-visible impact is low (ogres are uncommon hostile mobs and both effects are edge-case flavor), but the surveyor's 'adapted' label understates it: nothing was reimplemented in place of these — they were simply dropped. 'confirmed_partial' is the accurate status: the ogre is a faithful port overall with these two specific legacy behaviors missing.
- **Verified in port:** Port MoCEntityOgre.java (common/.../entity/monster/MoCEntityOgre.java): selectType() lines 55-68 pick cave ogre purely on `random.nextInt(100) >= 50` with NO sky/depth check; it does NOT override burnsInDaylight(). Parent MoCMob.java lines 174-199 provides a burnsInDaylight() hook (default false → ignite 8s when true), overridden by Wraith/undead-horse but NOT by Ogre. Grep across entire multiloader tree for canCaveOgre|isDaytime|canSeeSky within Ogre and for a depth/sky spawn gate returns no matches. Legacy MoCEntityOgre.onLivingUpdate lines 283-295 (health-=5 for type>2 in bright daylight under open sky) and canCaveOgreSpawn() lines 382-386 (!canBlockSeeTheSky && posY<50) have no counterpart in the port.

### L14. Horse saddle item (horsesaddle) is inert; horses need no saddle to ride
- **Area:** items — **verdict:** confirmed_partial
- **What’s missing/reduced:** The surveyor's core premise is wrong: saddle-gated riding is fully implemented in the port (rideNeedsSaddle=true for horses, mount blocked until isSaddled(), NBT "Saddled" persistence) -- a tamed adult horse must be saddled before it can be ridden, exactly as legacy required. The only real regression is narrower: the mod's dedicated "Crafted Saddle" item (drzhark.mocreatures HORSESADDLE) is dead inventory -- it has no interaction handler and no crafting recipe, whereas legacy let it (in addition to the vanilla saddle) make a horse rideable. Because the vanilla saddle fully covers the saddling function in the port, the missing item is a redundant/cosmetic alternative, so player-visible impact is minimal (severity low). To close the gap, either add HORSESADDLE to the saddle acceptance check in MoCAnimal.mobInteract (line 223: stack.is(Items.SADDLE) || stack.is(MoCItems.HORSESADDLE.get())) plus a crafting recipe, or intentionally drop the item as a deliberate adaptation. Files: MoCAnimal.java (223,231-232), MoCBehavior.java (49,114-116), MoCItems.java (239-240), MoCEntityHorse.java (388); legacy MoCItemHorseSaddle.java, legacy MoCEntityHorse.java:1328.
- **Verified in port:** The claim's headline ("horses need no saddle to ride") is FALSE in the port. MoCBehavior.java:114-116 registers horse with .ride(true), and ride(boolean needsSaddle) (MoCBehavior.java:49) sets rideNeedsSaddle=true. MoCAnimal.mobInteract gates mounting: line 231-232 mounts only when (!spec.rideNeedsSaddle || isSaddled()); line 223 saddles via stack.is(Items.SADDLE) -> setSaddled(true). State is synced (SADDLED accessor, MoCAnimal.java:67,344-349) and persisted as NBT "Saddled" (MoCAnimal.java:429,441). The horse's own mobInteract (MoCEntityHorse.java:303-388) falls through to super.mobInteract at line 388, so this saddle path is reachable for horses. Thus a tamed adult horse DOES require saddling before riding, matching legacy. What IS genuinely absent: the mod's own HORSESADDLE ("Crafted Saddle") item is inert. grep for horsesaddle across common/fabric/neoforge source returns ONLY the registry line (MoCItems.java:239-240, a plain new Item(...)), its item/model JSON, and its lang entry -- no interaction handler and no crafting recipe. Legacy accepted BOTH vanilla Item.saddle AND the mod horsesaddle: MoCEntityHorse.java:1328 (Item.saddle sets setRideable) plus MoCItemHorseSaddle.itemInteractionForEntity (horsesaddle sets rideable). The port only honors vanilla Items.SADDLE.

### L15. silversword / staff2 / staff3 registered as plain items with no behavior
- **Area:** items — **verdict:** confirmed_partial
- **What’s missing/reduced:** Split verdict. (1) staff2/staff3 as behaviorless items is FAITHFUL, not a gap: legacy's non-portal/non-teleport staff variants were always empty placeholder items (commented-out language names, no logic). The two staffs that DID have behavior (portal to Wyvern Lair + back, and raycast blink-teleport) are fully ported as MoCStaffPortalItem/MoCStaffTeleportItem. (2) silversword IS a real partial gap: it is the werewolf-slaying weapon in the 1.12.2 lineage but in the port it is just a reskinned iron sword with zero werewolf-specific effect, and the port werewolf has no weapon-based bonus-damage hook (the older aside legacy implemented that anti-werewolf bonus via gold tools). Player-visible impact is minor — silversword still functions as a normal iron-tier sword and werewolves are still killable — so severity is low. To close it faithfully, add a hurt() override on MoCEntityWerewolf that amplifies damage when the attacker holds SILVERSWORD (and, per the older behavior, gold tools). staff2/staff3 need no action.
- **Verified in port:** MoCItems.java:386-393 registers SILVERSWORD as `new Item(...sword(ToolMaterial.IRON, 3.0F, -2.4F))` (identical stats to SHARKSWORD line 382) and STAFF2/STAFF3 as bare `new Item(new Item.Properties()...)`. Grep for SILVERSWORD/STAFF2/STAFF3 across the entire port (common+fabric+neoforge) returns MoCItems.java ONLY — no recipes, no use logic, no entity interaction. The port werewolf MoCEntityWerewolf.java (164 lines) has NO hurt/attackEntityFrom override at all (methods list: defineSynchedData, isHumanForm, save/read, createAttributes, selectType, getTexture, sounds, applyHitEffects, customServerAiStep) — grep for silver/Silver/GOLD/swordGold returns nothing, so no weapon deals bonus damage vs werewolves. By contrast the two FUNCTIONAL staffs ARE fully ported: STAFFPORTAL->MoCStaffPortalItem and STAFFTELEPORT->MoCStaffTeleportItem (MoCItems.java:394-397; classes exist in item/). Legacy check: the _mocreatures_aside werewolf (MoCEntityWerewolf.java:182-214) gives bonus damage only to GOLD tools (swordGold=10 dmg), has no silversword; and legacy MoCreatures.java:826-829 shows plain `staff`/`staffender`/`staffunicorn`/`staffdiamond` were commented-out placeholder items with no behavior.

### L16. Raw meats lost hunger debuff; turtle soup doesn't return a bowl; whip drops bigcat/kitty branches
- **Area:** items — **verdict:** confirmed_partial
- **What’s missing/reduced:** All three sub-claims verified against the current live port. (1) Raw ostrich/rat/turkey/crab meats: the legacy ~80%-chance Hunger-30t debuff is genuinely GONE — the port registers them as plain food with no effect (also note port nutrition is 2/2/2/2 & sat 0.2, vs legacy 2/2/3/2 @ 0.3, a minor stat drift too). (2) Turtle soup: legacy returned an empty bowl on eating; port TURTLESOUP has no usingConvertsTo(Items.BOWL), so eating it yields nothing back — genuine regression. (3) Whip bigcat/kitty: the sit/stay TOGGLE for tamed bigcats and kitties survives via the generic fallback moc.setSitting(!isSitting()) at MoCWhipItem L89 (so mostly cosmetic-parity, though the legacy Kitty kittyState>2 && whipeable() guard is dropped — kitties now toggle unconditionally). The one behavior fully LOST is the legacy untamed-adult-bigcat aggro-on-crack (setTarget(player) on non-peaceful): the port's owns() predicate restricts the whip's entity query to tamed+owned creatures, so an untamed bigcat is never processed. Each item is individually low player-visible impact; verdict is confirmed_partial (present-but-reduced) rather than fully missing because the whip's core sit-toggle still reaches bigcats/kitties through the fallback path.
- **Verified in port:** Port MoCItems.java: CRABRAW (L160-161), OSTRICHRAW (L295-296), RATRAW (L307-308), TURKEYRAW (L402-403) all plain .food(nutrition(2).saturationModifier(0.2F)) — no .effect(); grep for effect(/MobEffectInstance/MobEffects.HUNGER in MoCItems = ZERO matches (legacy MoCreatures.java 611/621/628/679 set .setPotionEffect(Potion.hunger,30,0,0.8F)). TURTLESOUP L406-407 plain food nutrition(6) sat(0.7) — grep usingConvertsTo = ZERO matches (legacy MoCItemTurtleSoup.onEaten returns new ItemStack(Item.bowlEmpty)). MoCWhipItem.java applySpeciesEffect (L54-84) handles only Ostrich/Elephant/Horse/Wyvern — no BigCat/Kitty branch; useOn (L103) filters AABB to owns()=tamed+owned only, so untamed adult bigcat (legacy L73-77 setTarget(player) aggro) can never enter the loop.

### L17. Amulet capture drops persisted entity NBT (armor / saddlebags / chest contents / transform-essence state)
- **Area:** meta_critic — **verdict:** confirmed_partial
- **What’s missing/reduced:** The gap AS FRAMED is wrong. Legacy never persisted saddlebags, chest/howdah contents, golem inventories, or transform-essence state through the amulet — those NBT keys do not exist in MoCItemAmulet or dropAmulet, and the legacy amulet only ever worked on horses (getProperAmulet returns null otherwise; dropAmulet fires only from the ghost-horse VanishC mechanic, not player right-click). So the surveyor's high-severity claim of "permanent item loss of ALL chest/howdah contents" and "resets essence-transform progress" describes a regression that has no legacy counterpart to regress from. What IS genuinely reduced: legacy persisted two scalars the port omits — the horse's Armor value (setArmorType(armor)) and the Rideable flag. Everything else the port covers (health, age→adult, name, owner, type, variant/coat). Player-visible effect is limited to a horse losing its visible armour tier and rideable state across an amulet round-trip; there is no inventory/item-loss dimension, and no elephant/golem/transform dimension, because legacy amulets never touched those. Severity is low: the loss is two horse scalars (re-equippable armour, re-settable rideable), not the catastrophic permanent-item-loss the claim asserts. Fix if desired: add byte armorType + boolean rideable to CapturedCreature and restore via setArmorType/setRideable on release.
- **Verified in port:** PORT: common/.../item/CapturedCreature.java is a record of ONLY {typeId, variant, tamed, owner, health, age, customName}; MoCAmuletItem.java capture (interactLivingEntity, lines 40-53) writes exactly those 7 fields and release (use, lines 66-89) calls setTypeMoC/setTamed/setOwnerName/setMoCAge/setHealth/setCustomName — never armor or rideable. LEGACY: MoCItemAmulet.writeToNBT (lines 177-188) + MoCTools.dropAmulet (lines 1973-1981) persist {SpawnClass, Health, Edad, Name, Rideable, Armor(byte), CreatureType, Adult, OwnerName}, and MoCItemAmulet.onItemRightClick release (line 117) calls storedCreature.setArmorType(armor). Crucially MoCTools.getProperAmulet (lines 2002-2029) returns null for anything that is NOT a MoCEntityHorse, and dropAmulet is only ever invoked from MoCEntityHorse.VanishC vanish (line 2486) — the legacy amulet was HORSE-ONLY and never stored ChestedHorse, ChestItems, saddlebag contents, TransformCounter, EatenPumpkin, or any elephant/golem inventory.

### L18. Return from Wyvern Lair always goes to Overworld — origin dimension not preserved
- **Area:** meta_critic — **verdict:** confirmed_partial
- **What’s missing/reduced:** The surveyor is correct. The port hardcodes the Lair-return destination to Level.OVERWORLD and stores only a Vec3, discarding the origin dimension's ResourceKey. Legacy preserved the true entry dimension (portalDimension = thePlayer.dimension → transferPlayerToDimension). Two concrete regressions vs legacy: (1) a player who invokes the staff from the Nether/End/any modded dimension is returned to the Overworld, not their origin; (2) the captured position() Vec3 is a set of coordinates from that other dimension applied verbatim as Overworld coordinates with no rescaling (legacy stored posX/Y/Z as ints and re-applied them into whatever dimension it also returned to, so coordinates matched the dimension). Severity is low because in normal play the staff/portal loop is Overworld↔Lair (the wyvern_portal worldgen feature and the whole staff flow are Overworld-oriented), so the origin dimension is almost always the Overworld and the return lands correctly; the misbehavior only surfaces for the edge case of entering the Lair from a non-Overworld dimension. Not 'missing' (a return-position mechanism exists and works for the common case) and not a deliberate adaptation (nothing in the code or comments signals an intentional Overworld-only design; the comment on line 34 even literally assumes 'Where each player left the Overworld').
- **Verified in port:** common/src/main/java/drzhark/mocreatures/item/MoCStaffPortalItem.java: line 48 `ServerLevel target = server.getLevel(toLair ? WYVERN_LAIR : Level.OVERWORLD);` hardcodes the return level to OVERWORLD. The return-position store is line 35 `Map<UUID, Vec3> RETURN` — it holds ONLY a Vec3, never a ResourceKey<Level>. On departure line 55 `RETURN.put(serverPlayer.getUUID(), serverPlayer.position())` captures raw coords in whatever dimension the player was in; on return line 71-73 `Vec3 back = RETURN.remove(...); pos = back;` applies that Vec3 verbatim in the Overworld. A repo-wide grep for return/origin-dimension handling (portalDimension/originDimension/getLevel(Level.OVERWORLD)) across common/fabric/neoforge Java found no other logic — this item is the only return path. Legacy ItemStaffPortal.java captured `portalDimension = thePlayer.dimension` (line 71) and returned via `transferPlayerToDimension(thePlayer, portalDimension, ...)` (line 161).

## Deliberate adaptations (not gaps)

- **Custom network packet suite reduced to a single name payload; server->client FX/sync packets missing** (config_cmd_gui_hud): The literal wording of the claim is factually accurate (only one custom payload remains, and the ~19 other legacy packets are gone as explicit packets), but the implied feature loss is wrong. This is a deliberate 26.2 re-architecture, not a gap: server->client FX/state/health/transform/attach/golem-cube/shuffle are all carried by vanilla EntityData sync instead of manual packets; insta-spawn is superseded by /moc spawn; naming keeps the one retained C2S payload; and flying-mount jump/dive/steering are driven by MoCEntityWyvern.travel() from the rider's synced look+input rather than jump/dive C2S command packets. The surveyor's own status ("partial, low, most correctly replaced by vanilla data sync") already concedes this; the only residual player-visible concerns (horse spacebar power-jump charge and a dedicated dismount/dive keybind) belong to the separate keybind/riding audit, not the network-packet-suite claim. As a network audit item this is an adaptation, not a missing or partial feature. Note: I did not find a legacy-style horse jump-charge (PlayerJumpPendingScale/onPlayerJump) in the port's MoCEntityHorse (extends MoCAnimal), but that is a riding-controls question, not a packet-suite question.
- **FX_VACUUM golem particle stream** (client_render_fx): The surveyor is correct. The MoCEntityFXVacuum coloured-particle stream is genuinely absent (no fx_vacuum particle type, no JSON, no emission call), so at the literal level the specific FX is missing. But it was purely cosmetic: in the legacy source, openChest() (MoCEntityGolem line 935-958) already detected a nearby real MoCEntityThrowableRock and only THEN layered VacuumFX particles on top of it, gated behind the client particle-density setting (getParticleFX()). The port preserves that same underlying rock-into-chest mechanic with a visible flying block entity (MoCEntityThrowableRock homing into openChest), so the self-assembly reads visually to the player — a block is drawn into the golem's chest cavity. This is a deliberate reimplementation of the same visual intent with a solid entity rather than a coloured particle burst, not a lost feature. Player-visible impact is minimal (a subtle particle shimmer is gone; the actual absorbed block flying in is arguably more legible). Optional low-priority polish: register an fx_vacuum SimpleParticleType and emit it from a client-side tick when golemOpenChest is true to restore the legacy shimmer.

